# WordPress AI — Cross-Repo Dependencies & Repository Radar

> Live cross-repository signals that affect the `WordPress/ai` roadmap but are not counted in Project #240 totals: a curated Gutenberg/Abilities dependency watchlist plus full PR/issue/release censuses for the PHP AI Client, MCP Adapter, and Abilities API.
>
> | | |
> |---|---|
> | **Data snapshot** | 2026-08-23 17:04 UTC |
> | **Scope** | 16 tracked dependencies = 11 Gutenberg items + 5 abilities-api items; full PR/issue/release census for `WordPress/ai`, `WordPress/php-ai-client`, `WordPress/mcp-adapter`, and `WordPress/abilities-api` |
> | **State split** | 9 open · 4 closed · 3 merged *(**zero drift on every axis** — no membership, state, milestone, title, or activity change since the 2026-08-17 baseline)* |
> | **Membership source** | `wp-ai-roadmap-dependencies.json` (versioned registry; `WP_AI_DEPS_FILE` overrides) |
> | **Repository source** | `wp-ai-roadmap-repositories.json` (additional full-census repositories; `WP_AI_REPOS_FILE` overrides) |
> | **Source commands** | `./wp-ai-roadmap-refresh.sh census --strict` · `./wp-ai-roadmap-refresh.sh dependencies --strict --json` |

The dependency section intentionally tracks a **curated watchlist**, not every open Gutenberg or abilities-api issue. Gutenberg is too broad for whole-repo tracking to be useful here; the watchlist follows only items explicitly referenced by the WordPress AI roadmap, issue dossiers, or planned-work risk notes. The PHP AI Client, MCP Adapter, and Abilities API are narrower foundational repositories, so they receive a separate **full open-PR/issue/release census** without importing their issue backlogs as dossiers or imposing Project #240 coverage rules.

Watchlist **membership lives in `wp-ai-roadmap-dependencies.json`**, not in shell code. Each registry item declares `id` (`owner/repo#number`), `theme`, `aiRefs` (the WordPress AI issue numbers it affects, stored as integers), `note`, and `required`. Adding or removing a dependency is a reviewable JSON data change; the schema is validated on every run (`schemaVersion` 1, unique IDs, positive-integer `aiRefs`).

## Why This Exists

Project #240 is still the canonical source for `WordPress/ai` roadmap counts. The cross-repo watchlist captures upstream blockers and enabling work that can change the delivery path without changing Project #240:

- **Gutenberg**: Abilities and workflows, Guidelines/Skills, Media Editor, focal-point/media APIs, DataViews/admin UX, snackbar/toast accessibility.
- **abilities-api**: ability filtering, safety metadata, post-type CRUD abilities, core abilities scope, and ability meta.

## Full-Repository PR/Issue/Release Census

The refresh script enumerates every open PR, **every open issue**, and the release stream for these repositories on every normal/full census. Records include draft/review/merge/check state, activity timestamps, authoritative closing-issue references, milestone/label/assignee data, PR/issue/release diffs, per-repository validation, and independent snapshots.

**Issue coverage is census-level, not dossier-level.** Upstream issues are tracked as structured records and diffs — counts, state, milestone, labels, assignees, and what changed since the baseline. The narrative per-issue dossier treatment in [`wordpress-ai-open-issues.md`](./wordpress-ai-open-issues.md) remains exclusive to `WordPress/ai` board issues.

| Repository | Open PRs | Open issues | Latest shipped | Roadmap relevance |
|---|---:|---:|---|---|
| [`WordPress/ai`](https://github.com/WordPress/ai) | **30** | **52** *(+5)* | **1.3.0** (2026-08-18) | The plugin itself. **v1.3.0 shipped on 2026-08-18 at 21:04 UTC** — one day past target, 35 days after v1.2.0 — and shipped with #892's embedding changelog entry intact even though #946 had disabled the overlay (see below). Open PRs held at 30 while **thirteen merged and two opened**. Every open issue is also a Project #240 card — an *enforced* invariant (see below), not an observation. |
| [`WordPress/php-ai-client`](https://github.com/WordPress/php-ai-client) | **32** *(+3)* | **35** *(+1)* | **1.4.0** (2026-07-15) | Uniform provider-agnostic PHP client beneath WordPress AI features; changes to models, modalities, schemas, streaming, embeddings, and provider selection can alter the plugin's implementation path. Three new PRs, all zahidgh's, all peripheral to the embedding question. Open **#274** is still the gate on the plugin's embedding substrate — and now has an independent end-to-end verification on it. |
| [`WordPress/mcp-adapter`](https://github.com/WordPress/mcp-adapter) | **13** *(−1)* | **43** *(+2)* | **v0.6.1** (2026-08-13) | Abilities-to-MCP bridge; transport, exposure, approval, session, schema, and server-registration changes can alter external-agent integrations. The PR count fell by *resolving* two overlapping docs PRs, and two new fail-open defects arrived on the issue side. |
| [`WordPress/abilities-api`](https://github.com/WordPress/abilities-api) | **14** | **8** | **v0.4.0** (2025-10-30) | ⚠️ **Pending archival** — see below. Censused since 2026-08-10, and **unchanged on every axis across all four snapshots**. |

### ⚠️ `abilities-api` is a graveyard, and the census is how you can tell

Adding this repository was expected to surface active keystone work. It surfaced the opposite, which is more useful.

**The Abilities API shipped into WordPress Core (PHP) and Gutenberg (JavaScript) and landed in Core for 7.0, so this standalone repository has served its purpose.** Issue [**#160** "Should this repository be archived?"](https://github.com/WordPress/abilities-api/issues/160) (jonathanbossenger, 2026-01-29) drew unanimous maintainer agreement — justlevine "a very strong **yes**", Jameswlepage "I don't see any reason to keep this open anymore… it has served its purpose", jeffpaul concurring and asking dd32/desrosj to perform the archival on 2026-02-04. **Nearly seven months later it is still open and the repository is still live.**

That single issue explains the whole shape of the census: **no open PR here has been touched since 2026-01-21**, the eight open issues are mostly proposals from late 2025, and the last non-prerelease was v0.4.0 in October 2025. The 14 open PRs are not a backlog anyone intends to review — several (#85 `wp_query_abilities`, #115/#119 ability filtering, #150 input/output validation filters) speak directly to open roadmap questions (`WordPress/ai` #21, #354, #348) but are stranded in a repository awaiting deletion.

**Consequence for this document:** treat abilities-api numbers as *archaeology*, not as in-flight work. Real Abilities API development now happens in `wordpress-develop` and `gutenberg`, neither of which is censused here — `gutenberg` deliberately (too broad to be a useful signal), `wordpress-develop` not at all. The curated watchlist below remains the only tracking of Abilities work that still matters, and it is watching a repository that is itself frozen.

> **Do not sum the watchlist and the census.** `WordPress/abilities-api` now appears in both, deliberately and with different jobs. The **watchlist** carries curation the census cannot derive — `theme`, the `aiRefs` mapping to affected WordPress AI issues, a `note`, and `required` — and follows items through closure (3 of its 5 abilities-api items are CLOSED). The **census** tracks only the live open set, with no curation and no memory of closed work. Two items (`#38`, `#84`) are open issues and so appear in both.

**Movement in this window (2026-08-17 → 2026-08-23).** The plugin released; the watchlist did not move at all; and the one upstream item that gates a plugin capability is still open — but it finally has evidence on it.

- **v1.3.0 shipped on 2026-08-18, and the embedding entry shipped with it.** PR **#946** had asked that #892's changelog line be excluded from the release. The published notes keep it and credit both PRs: *"Brought the embedding code over from the PHP AI Client and load that conditionally so those using the AI plugin can start to take advantage of embedding generation ([#892](https://github.com/WordPress/ai/pull/892), [#946](https://github.com/WordPress/ai/pull/946))."* In `develop` the `SDK_Overlay` does not load and `generate_embeddings()` returns `WP_Error`, so a user reading the 1.3.0 notes will conclude the capability is available and it is not. The full checklist ran in under three hours (release PR #958 merged 20:19 UTC → checks 20:56 → local testing 21:00 → release 21:04 → WordPress.org 21:11 → [announcement](https://make.wordpress.org/ai/2026/08/18/whats-new-in-ai-1-3-0/) 22:32), and the 1.3.0 milestone closed at **71 items**.
- **⚠️ `php-ai-client#274` is still the single most consequential item on this radar — and it has been open ten days.** Opened 2026-08-13, still **OPEN, BLOCKED, `REVIEW_REQUIRED`**, last touched 2026-08-22. It is the gate on re-enabling a merged plugin capability, and it is step one of `WordPress/ai#962`, the tracking issue filed on 2026-08-20 to sequence the embedding programme (assigned dkotter, who confirmed on the thread that the plugin waits on #274's approval). **What changed is that somebody tested it.** ColinM-sys ran #274 end to end against a local Ollama on 2026-08-22 — unit suite green on PHP 7.4–8.5, **36/36 live cases passing**, and the no-model auto-selection #274 removes reproduced on `trunk` — and surfaced the migration cost: **#274 removes `usingProvider()` and `usingModelPreference()` from `EmbeddingBuilder` in favour of a required `usingProviderModel()`**, while `WordPress/ai`'s `generate_embeddings()` calls both and the #891/#943 semantic-search callers pass `model_preference`. **Re-enabling embeddings in the plugin is therefore a caller migration, not a revert of #946.** That is the sharpest example this document has of why the census matters: an upstream contract change that reads as "one PR to merge" is actually a rewrite of three call sites in another repository.
- **`php-ai-client` +3 → 32 PRs / +1 → 35 issues / 1.4.0 — none of it about embeddings.** Nothing merged or closed. All three new PRs are zahidgh's: **#278** documents the HTTPlug/PSR-18 adapter requirement immediately after installation (without an adapter the client cannot discover an HTTP transport at all — a first-run failure the README did not explain), **#279** lets `TokenUsage` fields and `GenerativeAiResult.usage` be null so providers stop manufacturing zero values (`Closes #158`), and **#280** adds a protected no-op hook so a provider can normalize its own `FunctionDeclaration`s before serialization, since provider APIs accept different JSON Schema subsets for tool parameters. New issue **#277** (juanlentino) is the one to watch downstream: **`MessagePart` can carry exactly seven things and none of them is a citation**, so Anthropic's `citations`, OpenAI's `annotations`, and Gemini's `citationMetadata` are all silently dropped. Any plugin feature that wants to show sources — RAG, frontend chat #142, semantic search #844 — inherits that gap. **#275** (provider-plugin updates) is still open and, unusually here, **CLEAN**. Last window's 82-file draft **#276** has left the open set.
- **`mcp-adapter` −1 → 13 PRs / +2 → 43 issues — the count fell by resolving, not merging.** **#291** (deprecation information → `docs/README.md`, steviepee) **merged 2026-08-22 01:55**, and **#292** (dedupe the root README, justlevine) was **closed unmerged three minutes earlier** — the two overlapped and one was dropped. One opened: dependabot **#296** (`@wordpress/scripts` 32.6.0 → 33.0.0). **Two new defects, both fail-open, both in the path this plugin depends on:** **#297** — `McpAbilityHelperTrait::get_ability_mcp_type()` validates `meta.mcp.type` against `tool`/`resource`/`prompt` and **returns `'tool'` for anything unrecognised**, so a typo or a future type silently becomes an executable tool rather than an error; and **#298** — `ToolsHandler::create_error_result()` forwards only `WP_Error::get_error_message()` and hardcodes `structuredContent => null`, discarding the `error_code`/`error_data` that are already logged server-side, leaving an MCP client no machine-readable way to distinguish a permission failure from a validation failure. Readiness churn on four PRs; both DTO-successor drafts (**#281**, **#287**) went BLOCKED → DIRTY and now need rebases. No release since v0.6.1.
- **`abilities-api` unchanged on every axis, in every snapshot it has ever had.** 14 PRs / 8 issues / v0.4.0 across all four censuses since 2026-08-10 — no PR opened, closed, merged, or changed readiness; no issue opened or closed; no release. For a repository whose archival has had unanimous agreement since 2026-01-29, that is the expected reading, and it is now measured rather than assumed.
- **Watchlist: zero drift, on every axis.** 16 items, 9 open / 4 closed / 3 merged. No membership change, no state change, no milestone change, no title change, and — unlike last window — **not even an activity bump**: every tracked item's `updatedAt` is identical to the 2026-08-17 baseline. `gutenberg#75221` (Media Editor focal-point selector) holds at 2026-08-16 after its one-day wake-up, and `gutenberg#77230` (Guidelines built on Knowledge in WP 7.2) holds at 2026-07-11 — **while its plugin-side consequence landed anyway**: `WordPress/ai` PR **#949** ports Gutenberg's `lib/experimental/knowledge` and `routes/guidelines` into the plugin, which is the first time a watchlist item's downstream work has moved without the item itself moving.

Project #240 coverage validation remains exclusive to the primary `WordPress/ai` repository. A PHP AI Client, MCP Adapter, or Abilities API PR or issue may be important upstream work without requiring a Project #240 card, and none of them can produce a coverage diagnostic.

### Board issue coverage (primary repository only)

The issue census makes the issue-side analogue of the PR coverage audit possible. Every open `WordPress/ai` issue is joined to Project #240 by `repository#number`; one with no card is an **`issue-roadmap-coverage-missing` error**, so `--strict` exits `2` — the same treatment an uncarded substantive PR already gets.

**Current state: 52 of 52 open issues carded, zero uncarded** — fourth consecutive window at 100%, through a window that added seven issues (**#951**, the five C2PA follow-ups **#953–#957**, and **#962**) and closed two. The rule is deliberately scoped to `WP_AI_REPO` and enforced at the call site, so the **86** upstream issues counted above can never be reported as missing roadmap cards.

⚠️ **The PR-side audit has no equivalent guarantee, and this window shows why that matters.** Open-PR coverage reads **1 unexplained of 30** — its best result ever — while **nine of the thirteen `WordPress/ai` PRs that merged this window had no board card at all**, including the 1.3.0 release PR (#958) and #947, which *closed a board issue*. A point-in-time audit of the open queue improves when uncarded work ships rather than waits. Issue coverage is enforced because filing an issue is how work enters the board; a PR can be opened, reviewed, and merged without anyone touching the tracker.

## Dependency Watchlist

| Dependency | Type | State | Milestone | Theme | AI refs | Updated |
|---|---:|---:|---|---|---|---:|
| [`WordPress/abilities-api#38`](https://github.com/WordPress/abilities-api/issues/38) | Issue | OPEN | Later | Ability registry filtering | #21, #354 | 2025-10-15 |
| [`WordPress/abilities-api#62`](https://github.com/WordPress/abilities-api/issues/62) | Issue | CLOSED | pre WP 6.9 | Ability safety metadata | #40 | 2025-10-09 |
| [`WordPress/abilities-api#84`](https://github.com/WordPress/abilities-api/issues/84) | Issue | OPEN | Later | Core CRUD abilities | #40 | 2025-10-30 |
| [`WordPress/abilities-api#105`](https://github.com/WordPress/abilities-api/issues/105) | Issue | CLOSED | pre WP 6.9 | Core abilities scope | #40 | 2025-10-24 |
| [`WordPress/abilities-api#106`](https://github.com/WordPress/abilities-api/issues/106) | Issue | CLOSED | WP 6.9 | Ability metadata | #40 | 2025-10-24 |
| [`WordPress/gutenberg#16549`](https://github.com/WordPress/gutenberg/issues/16549) | Issue | OPEN | — | Admin UX / accessibility | #699 | 2026-02-23 |
| [`WordPress/gutenberg#70710`](https://github.com/WordPress/gutenberg/issues/70710) | Issue | OPEN | — | Platform / workflows | #21, #40, #430 | 2026-06-22 |
| [`WordPress/gutenberg#72734`](https://github.com/WordPress/gutenberg/issues/72734) | Issue | OPEN | — | Media Editor | #325 | 2026-08-13 |
| [`WordPress/gutenberg#73771`](https://github.com/WordPress/gutenberg/issues/73771) | Issue | CLOSED | — | Media Editor | #238, #325 | 2026-08-12 |
| [`WordPress/gutenberg#74234`](https://github.com/WordPress/gutenberg/pull/74234) | PullRequest | OPEN | — | Platform / core abilities | #40 | 2026-06-19 |
| [`WordPress/gutenberg#74572`](https://github.com/WordPress/gutenberg/pull/74572) | PullRequest | MERGED | Gutenberg 22.7 | Admin UX / DataViews | #741 | 2026-02-26 |
| [`WordPress/gutenberg#75221`](https://github.com/WordPress/gutenberg/issues/75221) | Issue | OPEN | — | Media / focal point | #238 | 2026-08-16 |
| [`WordPress/gutenberg#77230`](https://github.com/WordPress/gutenberg/issues/77230) | Issue | OPEN | — | Skills / Guidelines | #430 | 2026-07-11 |
| [`WordPress/gutenberg#77643`](https://github.com/WordPress/gutenberg/pull/77643) | PullRequest | MERGED | Gutenberg 23.1 | Skills / Guidelines | #430 | 2026-04-29 |
| [`WordPress/gutenberg#77816`](https://github.com/WordPress/gutenberg/issues/77816) | Issue | OPEN | — | Admin UX / toast component | #699 | 2026-06-29 |
| [`WordPress/gutenberg#77994`](https://github.com/WordPress/gutenberg/pull/77994) | PullRequest | MERGED | Gutenberg 23.2 | Media Editor | #325 | 2026-06-25 |

## Refresh Notes

Run the normal refresh to include this watchlist in the JSON/Markdown report:

```bash
./wp-ai-roadmap-refresh.sh
```

Run the dependency-only path when you only need the cross-repo state:

```bash
./wp-ai-roadmap-refresh.sh dependencies
./wp-ai-roadmap-refresh.sh dependencies --json
./wp-ai-roadmap-refresh.sh dependencies --strict --json   # audit mode
```

Run the repository-only path for all four full PR/issue/release censuses:

```bash
./wp-ai-roadmap-refresh.sh census
./wp-ai-roadmap-refresh.sh census --strict
./wp-ai-roadmap-refresh.sh issuediff A.json B.json   # offline issue diff
```

Use `--save` on the normal refresh to persist the dependency snapshot plus independent `prs-<owner>-<repo>-*.json`, `issues-<owner>-<repo>-*.json`, and `releases-<owner>-<repo>-*.json` baselines for every available repository, so later runs can report state, readiness, issue, and release changes. A full `--save` now writes **14** files: `proj240` + (`prs`, `issues`, `releases`) × 4 repositories + the dependency snapshot.

The issue census is independently fail-soft. A failed issue fetch sets `issues_available: false`, records an `issue-census-unavailable` error, and leaves that repository's PR and release data intact — and it **skips the issue snapshot** rather than writing an empty array over a good baseline, which would otherwise report every issue as closed on the next run.

### Fetch behavior, UNKNOWN placeholders, and strict mode

- Every registry item is attempted on every run. A failed GitHub request for a **required** item is retried exactly once (two attempts total); **optional** items get one attempt.
- An unreachable item never disappears from the report. It is emitted as a placeholder with `state: "UNKNOWN"`, its configured theme/refs/note, and a structured `fetchError` (`{code, endpoint, attempts, message}`). The dependency diff therefore reports a temporary outage as a state change to `UNKNOWN`, not as a removed dependency — registry removal is the only thing that removes an item.
- The JSON result carries `validation` (`{ok, errors, warnings}`): a required `UNKNOWN` item is an error, an optional `UNKNOWN` item is a warning, and an invalid registry produces `dependency-registry-invalid` errors with an empty item list.
- Normal mode is warning-only and always exits `0` when a report was produced. `dependencies --strict --json` emits the same JSON first and then exits `2` when any validation error exists (exit `1` is an operational failure, e.g. an unreadable registry file).
- `aiRefs` are stored as integers in the registry and new snapshots; historical snapshots with `"#NNN"` strings remain readable, and renderers always print exactly one `#` prefix either way.

## Changelog

| Date | Change |
|---|---|
| 2026-08-23 | **Live census + watchlist vs the 2026-08-17 20:39 UTC baseline — the plugin released, the watchlist did not move at all, and the embedding gate acquired evidence.** Census: `WordPress/ai` **30 PRs / 52 issues *(+5)* / 1.3.0 (2026-08-18)** — **v1.3.0 shipped one day past target and 35 days after v1.2.0**, with #892's embedding changelog entry **kept and reworded to say the code loads "conditionally"**, crediting #946, the PR that switched it off; the overlay still does not load in `develop`. `php-ai-client` **29 → 32 PRs / 34 → 35 issues** — three new PRs, all zahidgh's (**#278** HTTPlug/PSR-18 adapter docs, **#279** nullable `TokenUsage` closing #158, **#280** a provider hook for normalizing `FunctionDeclaration`s), plus new issue **#277**: `MessagePart` carries seven fields and none is a citation, so Anthropic `citations`, OpenAI `annotations`, and Gemini `citationMetadata` are all dropped — a gap inherited by any plugin feature that wants to show sources. `mcp-adapter` **14 → 13 PRs / 41 → 43 issues** — the count fell by *resolving*: **#291** merged 2026-08-22 and overlapping **#292** was closed unmerged three minutes earlier; dependabot **#296** opened; new **#297** (unknown `meta.mcp.type` coerces to `'tool'` — fails open) and **#298** (`create_error_result()` discards `WP_Error` code/data, leaving MCP clients no machine-readable failure type); drafts #281/#287 both BLOCKED → DIRTY. `abilities-api` **unchanged on every axis in all four snapshots since it entered the census on 2026-08-10** — measured now, not assumed. **`php-ai-client#274` has been open ten days (since 2026-08-13), still BLOCKED and `REVIEW_REQUIRED`, and still the gate on a merged plugin capability** — but ColinM-sys tested it end to end against a local Ollama on 2026-08-22 (PHP 7.4–8.5 green, 36/36 live cases, the auto-selection it removes reproduced on `trunk`) and established that it drops `usingProvider()`/`usingModelPreference()` from `EmbeddingBuilder`, which `generate_embeddings()` and the #891/#943 callers both use: **re-enabling embeddings is a caller migration, not a revert of #946.** It is now step one of `WordPress/ai#962`, the tracking issue filed 2026-08-20 and assigned dkotter. Watchlist: **16 items, 9 open / 4 closed / 3 merged, zero drift on every axis** — no membership, state, milestone, title, or even activity change; every `updatedAt` is identical to the baseline. Notable anyway: `gutenberg#77230` (Guidelines built on Knowledge in WP 7.2) has not moved since 2026-07-11, **while its plugin-side consequence landed** as `WordPress/ai` PR **#949**, which ports `lib/experimental/knowledge` and `routes/guidelines` into the plugin — the first time downstream work moved without the tracked item moving. Board issue coverage **52 of 52 carded** (fourth window at 100%), with a new note on why the PR-side audit's best-ever reading (1 of 30) is misleading: nine of thirteen merges this window were uncarded, including the release PR. Registries unchanged; no script changes. **Closing pull at 17:04 UTC:** every census and watchlist figure in this document is unchanged from the 07:41 read — 30/52, 32/35, 13/43, 14/8, the same four releases, and the same 16 items at zero drift. The only movement anywhere across the four repositories was `WordPress/ai` PR **#929** leaving draft, which touches no count here. |
| 2026-08-17 | **Live recheck vs the 2026-08-15 00:31 UTC baseline — the watchlist barely moved and the radar delivered its most consequential finding yet.** Dependency membership holds at **16** (11 Gutenberg + 5 abilities-api) and the state split is unchanged at **9 open · 4 closed · 3 merged**, with no membership, state, milestone, or title drift and no required item UNKNOWN. **One activity bump:** `gutenberg#75221` (Media Editor focal-point selector) **2026-02-04 → 2026-08-16**, its first movement in six months — and its mapped card `WordPress/ai#238` was touched the same day. **The radar's headline: the plugin is backing out the embedding vendoring on release day, for exactly the upstream reason this file logged two windows ago.** `WordPress/ai` PR **#946** (dkotter, 2026-08-17 17:27 UTC, repo-milestoned 1.3.0) disables loading of the `SDK_Overlay` class that **#892** added, because *"there are some changes we'd like to see with this embedding integration on the PHP AI Client side and those are being handled upstream"* in **`php-ai-client#274`** — the same PR this file flagged on 2026-08-14 as proof the vendoring and the upstream contract were "already drifting one patch apart." `generate_embeddings()` now returns `WP_Error`, dependent tests are skipped, and #892's changelog entry is to be excluded from the 1.3.0 release; the change is explicitly temporary, gated on #274 merging. **And it landed during this refresh:** opened 17:27, approved by jeffpaul 18:22, merged by dkotter 18:54 — **87 minutes, uncarded throughout**, and the only approving review any open `WordPress/ai` PR has drawn in nine windows was spent on switching a shipped capability off. Because it opened and merged between two snapshots it leaves no diff row anywhere in this tooling. Consequence worth tracking: **#683**, **#844**/**#891**, and new **#943** are all building on a substrate that no longer loads in `develop`, so #274 is now the blocker on re-enabling a merged plugin feature. **`php-ai-client` 28 → 29 / 34 / 1.4.0 — and the one PR that would have mattered is not among the movement:** #274 and #275 both still open, nothing merged, closed, or re-milestoned. The gain is draft **#276** (JasonTheAdams, 17:50 UTC), a mechanical `token_get_all()` pass qualifying global function/constant references across **82 files** in `src/`/`tests/` plus a new `ReferenceUsedNamesOnly` phpcs rule — no behavior change, but a wide rebase hazard sitting in front of the embedding work. **`mcp-adapter` 12 → 14, entirely documentation:** **#285** (readme.txt rebuild, abhi3315) **merged 2026-08-16**; opened **#288** (survive a second active copy + Composer-migration docs, part of #178), **#291** (deprecation info → `docs/README.md`), **#292** (dedupe root README); draft **#281** went **BLOCKED → DIRTY**, so both DTO-successor drafts now need rebases. Issues flat at 41; no release anywhere since v0.6.1. **`abilities-api` static** at **14 / 8 / v0.4.0**. Primary `WordPress/ai` movement (board flat at 279 with one status change, census **28 → 30** open PRs, coverage gap **1 → 4** during the window and 3 at its close) is recorded in the roadmap and planned-work files, not counted as cross-repo drift. **Data-collection note:** GitHub's GraphQL API returned intermittent HTTP 503s during the evening's earlier pulls, and two of them lost a census entirely (`abilities-api`, then the primary `WordPress/ai` repo, the latter exercising the fail-soft path exactly as designed — board-plus-three-repos report, one `repository-census-unavailable` error, no snapshot overwritten); the **20:39 UTC census these figures come from reached all four repositories cleanly**, so every snapshot in this window rolls forward together. A second collection caveat is worth recording because it bit this window: the census reads `mergeStateStatus` from a **list** query, and GitHub answers those from cache — the 18:49 pull reported #946 `BLOCKED` for nearly half an hour after its approval had already made it `CLEAN`, which a forced single-PR query and the REST `mergeable_state` both confirmed, and it merged 5 minutes after that. GitHub-side lag, not a script defect (the script's retry covers `UNKNOWN`, a genuinely uncomputed answer), but a merge-state that has just changed can trail the census. |
| 2026-08-14 | **Live recheck vs the 2026-08-10 11:11 UTC baseline. The watchlist changed state for the first time in four windows; the radar recorded its first releases since April.** Dependency membership holds at **16** (11 Gutenberg + 5 abilities-api), but the state split moved **10 open / 3 closed / 3 merged → 9 open / 4 closed / 3 merged**: **`WordPress/gutenberg#73771`** (the Media Editor modal task tracker) went **OPEN → CLOSED**, with Updated cells bumped on **#73771** (→ 2026-08-12) and **#72734** (Media Library dedicated media editor, → 2026-08-13). No membership, milestone, or title drift; no required item UNKNOWN. **Radar: `WordPress/mcp-adapter` shipped v0.6.0 (2026-08-12) and v0.6.1 (2026-08-13)** — its first releases since April — as the DTO-normalization cluster merged out (#171, #223, #252, #263; five more PRs closed unmerged: #173/#217/#218/#238/#264); open PRs **18 → 12**, open issues **46 → 41** (new #280 real-HTTP-transport; six closed with the merged work). Three PRs opened: draft **#281** (exact-revision 2026 `tools/call`) and draft **#287** (descriptor-backed dual-revision runtime) restate the DTO question, plus **#285** (readme.txt rebuild). `php-ai-client` **26 → 28** on **#274** (force an explicit model for embeddings) and **#275** (provider-plugin updates); issues **35 → 34** with #215 closed and streaming/embeddings/custom-auth items re-milestoned. The standing cross-repo consequence finally resolved: `WordPress/ai#892` — the `SDK_Overlay` vendoring of 1.4.0 embedding support — merged under 1.3.0, one window before php-ai-client #274 moved the embedding contract beneath it. Primary `WordPress/ai` movement (board 269 → 279, census 36 → 28 open PRs, 1.3.0 draining to its release checklist, unexplained 6 → 1) is recorded in the roadmap and planned-work files, not counted as cross-repo drift. |
| 2026-08-10 | **Coverage expansion: issue censuses for every tracked repository, plus `WordPress/abilities-api`.** The radar previously counted open PRs and releases but never issues, and omitted the Abilities API entirely — leaving ~42 open PRs and ~111 open issues in WordPress-org AI repositories outside these documents. The census now enumerates **open PRs, open issues, and releases** for all four tracked repositories: `WordPress/ai` (36 PRs / 50 issues), `php-ai-client` (26 / 35), `mcp-adapter` (18 / 46), and newly added `abilities-api` (14 / 8). Upstream issue coverage is **census-level** — records, counts, and diffs (newly opened / no longer open / milestone, title, label, assignee changes) — not dossiers, which stay exclusive to `WordPress/ai` board issues. **New audit:** every open `WordPress/ai` issue is joined to Project #240, and an uncarded one is now an `issue-roadmap-coverage-missing` **error** that fails `--strict`; the current state is **50 of 50 carded**, a claim this file previously asserted and now verifies each run. The rule is primary-repository-only and enforced at the call site, so no upstream issue can produce a coverage diagnostic. **The first abilities-api census immediately paid for itself, in the opposite direction from the one expected:** the repository is **pending archival** — the Abilities API shipped into Core and Gutenberg, and issue **#160** (2026-01-29) carries unanimous maintainer agreement to archive, with jeffpaul asking dd32/desrosj to action it on 2026-02-04. Six months later it is still live, no open PR has been touched since 2026-01-21, and its 14 PRs (including #85/#115/#119 ability filtering and #150 validation filters, all bearing on roadmap issues #21/#354/#348) are stranded. Treat abilities-api numbers as archaeology; real Abilities work has moved to `wordpress-develop` and `gutenberg`, neither censused here. The watchlist and the census now overlap on abilities-api by design and **must not be summed**. Snapshots per `--save` go 8 → **14** (`issues-<slug>-*` added for each repository); a failed issue fetch degrades that repository's issue half only and skips its snapshot rather than overwriting a good baseline. Design and plan: `docs/superpowers/specs/2026-08-10-issue-census-design.md`, `docs/superpowers/plans/2026-08-10-issue-census.md`. Offline suite 7 → **8** files (new `-issues.sh`). |
| 2026-08-10 | **Live recheck vs the 2026-08-01 14:58 UTC baseline. Watchlist static for a third straight window; the radar reversed direction.** No dependency membership, state, milestone, title, or activity change: 16 items hold at **10 open · 3 closed · 3 merged** (11 Gutenberg + 5 abilities-api), every `Updated` cell steady, no required item UNKNOWN, and an empty diff on all six axes — the deps snapshot was rolled forward only to keep baselines aligned with the board/census files. **Radar: the two repositories moved opposite ways for the first time.** `WordPress/php-ai-client` **24 → 26** (nothing merged or closed; new **#271** `isSupported()` returning false instead of throwing on an unmatched output modality, and **#273** adding an *automatic function-call resolution loop* plus `withMessages()` to `PromptBuilder` — an agentic primitive landing one layer beneath the plugin, the same position embeddings held before #892 vendored them). **First approvals the radar has ever recorded:** php-ai-client **#231**, **#250**, and **#254** all reached APPROVED (#231 and #254 also BLOCKED → CLEAN) — against zero approvals on all 36 open `WordPress/ai` PRs. `WordPress/mcp-adapter` **20 → 18**, its first decline since joining the census: **#262** (emit `mimeType` as declared) **merged 2026-08-05** and **#256** (pre-tool-call completion) **closed unmerged 2026-08-01**; **#277** opened and merged within this same working day. **Last window's overlap prediction landed:** with the narrow #262 merged, the comprehensive draft **#260** — which also declares `Closes #245` — went **BLOCKED → DIRTY**, exactly the collision flagged when the cluster was described as overlapping rather than partitioning; #263 and #264 stay open against the same issue. Neither repository has cut a release for a third consecutive window (php-ai-client 1.4.0, July; mcp-adapter v0.5.0, April — now nearly four months old). Primary `WordPress/ai` movement (board flat at 269 with 6 in / 6 out, census 33 → 36 open PRs, unexplained holding at 6 with half the set turned over) is recorded in the roadmap and planned-work files, not counted as cross-repo drift. |
| 2026-08-01 | **Live recheck vs the 2026-07-27 08:51 UTC baseline. Watchlist static for a second straight window; the radar keeps climbing.** No dependency membership, state, milestone, title, or activity change: 16 items hold at **10 open · 3 closed · 3 merged** (11 Gutenberg + 5 abilities-api), every `Updated` cell steady, no required item UNKNOWN, and an empty diff on all six axes — the deps snapshot was rolled forward only to keep baselines aligned with the board/census files. **Radar: `WordPress/php-ai-client` 21 → 24** (new **#266** Guzzle error-response preservation, **#267** `file_exists()` on oversized strings, and **#269** *text extraction / OCR / document parsing*, +1,618 lines — a new client-level modality worth tracking downstream; nothing merged or closed, no readiness changes) and **`WordPress/mcp-adapter` 16 → 20** (merged **#251** concurrent-session overwrites and **#254** inherit public ability exposure; opened **#258** resource meta, **#259** nullable singletons/WP-CLI alias, and a single DTO-normalization workstream counted four times — draft **#260** plus overlapping same-scope cuts **#262** `mimeType`, **#263** `_meta` propagation, **#264** malformed-annotation hardening, all four against upstream issue #245; **#244** readiness BLOCKED → DIRTY). **Resolution worth recording:** last window flagged mcp-adapter #254 as the upstream half of the ability-exposure question — it merged on 2026-07-27, so MCP exposure now inherits public ability exposure upstream while the plugin-side toggle (`WordPress/ai#863`, PR #881) remains undecided. Neither repository has cut a release since the last window (php-ai-client 1.4.0 in July, mcp-adapter v0.5.0 in April); the adapter is now carrying 20 open PRs against a nearly four-month-old release. Primary `WordPress/ai` movement (board 274 → 269, census 32 → 33 open PRs, unexplained 3 → 6) is recorded in the roadmap and planned-work files, not counted as cross-repo drift. |
| 2026-07-27 | **Live recheck vs the 2026-07-20 06:15 UTC baseline. Watchlist completely static; repository radar moved.** No dependency membership, state, milestone, title, or activity change: 16 items hold at **10 open · 3 closed · 3 merged** (11 Gutenberg + 5 abilities-api), every `Updated` cell steady, and no required item UNKNOWN — the diff was empty on all six axes, and the deps snapshot was rolled forward only to keep baselines aligned with the board/census files. **The radar is where the movement is:** `WordPress/php-ai-client` **20 → 21** (new **#264**, model context-window metadata + proactive token-limit check; readiness change on **#254**, unmet-required-options vs unsupported-capability errors) and `WordPress/mcp-adapter` **12 → 16** (new **#251** concurrent-session overwrites, **#252** websocket-driver 0.7.5, **#254** inherit public ability exposure, **#256** pre-tool-call completion) — the adapter's biggest single-window jump since joining the census, with no release since v0.5.0 in April. **Cross-repo consequence worth flagging:** php-ai-client's shipped 1.4.0 embeddings support, combined with the WP 7.1 → 7.2 slip of `wordpress-develop#12530`, is why `WordPress/ai` PR **#892** now vendors that embedding code into the plugin behind an `SDK_Overlay` — an upstream release-timing decision reshaping the plugin's own architecture, which is exactly the class of signal this file exists to catch. Primary `WordPress/ai` movement (board 283 → 274, census 37 → 32 open PRs) is recorded in the roadmap and planned-work files, not counted as cross-repo drift. |
| 2026-07-20 | **Live recheck vs the 2026-07-18 16:34 UTC baseline.** No dependency membership, state, milestone, title, or activity changes: the watchlist holds at **16** (10 open · 3 closed · 3 merged; 11 Gutenberg + 5 abilities-api), every `Updated` cell steady, and no required item UNKNOWN. Upstream repository radar also unchanged — `WordPress/php-ai-client` **20 / 1.4.0** and `WordPress/mcp-adapter` **12 / v0.5.0**. Primary `WordPress/ai` movement (board PR #858 merge DIRTY → BLOCKED) is recorded in the roadmap and planned-work files, not counted as cross-repo drift. |
| 2026-07-18 | **Same-day live recheck vs the 2026-07-18 02:20 UTC baseline.** No dependency membership, state, milestone, title, or activity changes: the watchlist remains 16 (10 open · 3 closed · 3 merged; 11 Gutenberg + 5 abilities-api), with no required item UNKNOWN. The upstream repository radar is also unchanged at `WordPress/php-ai-client` **20 / 1.4.0** and `WordPress/mcp-adapter` **12 / v0.5.0**. Primary `WordPress/ai` movement is documented in the roadmap and planned-work files rather than counted as cross-repo drift. |
| 2026-07-18 | **Added full PR/release census tracking for two foundational repositories.** `WordPress/php-ai-client` starts at **20 open PRs / release 1.4.0 (2026-07-15)**; `WordPress/mcp-adapter` starts at **12 / v0.5.0 (2026-04-15)**. Membership lives in `wp-ai-roadmap-repositories.json`; both receive validation, diffs, rendering, and independent snapshots. They remain census-only and do not affect Project #240 coverage or the 16-item dependency watchlist. |
| 2026-07-17 | **Tooling: watchlist membership moved to the `wp-ai-roadmap-dependencies.json` registry** (schema-validated; `required` flag per item; integer `aiRefs`). Fetches are now resilient — required items retry once and unreachable items surface as `UNKNOWN` placeholders with a `fetchError` instead of vanishing — and `dependencies --strict --json` exits `2` when a required item is unreachable or the registry is invalid. Watchlist data itself unchanged (16 dependencies; 10 open · 3 closed · 3 merged). |
| 2026-07-17 | Live refresh vs the 2026-07-13 baseline snapshot. **No state, milestone, or list changes** — watchlist remains 16 dependencies (10 open · 3 closed · 3 merged; abilities-api 5, Gutenberg 11). `WordPress/gutenberg#73771` was retitled "Media Editor Modal task tracking" → "WordPress 7.1 Iteration: Media Editor Modal task tracking" and its Updated cell advanced 2026-07-08 → 2026-07-17. |
| 2026-07-12 | Live refresh vs the 2026-07-09 baseline snapshot. **No state, milestone, or list changes** — watchlist holds at 16 dependencies (10 open · 3 closed · 3 merged; abilities-api 5, Gutenberg 11). **1 title change + 1 activity bump**, both on `WordPress/gutenberg#77230`: retitled "Guidelines built on Knowledge in WordPress **7.1**" → "…WordPress **7.2**", and its Updated cell bumped 2026-07-01 → 2026-07-11. Context: the `WordPress/ai` **v1.2.0** lane advanced (6 more board-Done, incl. the #508 "Suggest Reply" experiment) and two more core-ability PRs opened upstream-adjacent (#856 `core/read-settings` snapshot ordering, #858 `core/read-nav-menus`), but no watchlisted Gutenberg/abilities-api dependency changed state. |
| 2026-07-09 | Live refresh vs the 2026-07-03 baseline snapshot. **No state, milestone, title, or list changes** — watchlist holds at 16 dependencies (10 open · 3 closed · 3 merged; abilities-api 5, Gutenberg 11). 1 item had new upstream activity; bumped Updated cell: `WordPress/gutenberg#73771` → 2026-07-08. Context: the `WordPress/ai` **v1.2.0** lane opened its first 10 board-Done items this window (incl. the new `core/read-users` Ability, PR #774, merged), but no watchlisted Gutenberg/abilities-api dependency changed state. |
| 2026-07-03 | Live refresh vs the 2026-07-02 baseline snapshot. **No state, milestone, title, list, or activity changes** — watchlist holds at 16 dependencies (10 open · 3 closed · 3 merged; abilities-api 5, Gutenberg 11). No `Updated` cells bumped (no upstream activity in the window). Context: `WordPress/ai` de-carded 7 already-Done issues from Project #240 this window, but no watchlisted Gutenberg/abilities-api dependency changed. |
| 2026-07-02 | Live refresh vs the 2026-06-30 baseline snapshot. **No state, milestone, title, or list changes** — watchlist holds at 16 dependencies (10 open · 3 closed · 3 merged). 2 items had new upstream activity; bumped Updated cells: `WordPress/gutenberg#73771` → 2026-07-01, `#77230` → 2026-07-01. Context: `WordPress/ai` shipped **v1.1.0** (2026-07-01) in this window, but no watchlisted Gutenberg/abilities-api dependency changed state. |
| 2026-06-30 | Live refresh vs the 2026-06-26 baseline snapshot. **No state, milestone, title, or list changes** — watchlist holds at 16 dependencies (10 open · 3 closed · 3 merged). 3 items had new upstream activity; bumped Updated cells: `WordPress/gutenberg#73771` → 2026-06-30, `#77230` → 2026-06-30, `#77816` → 2026-06-29. |
| 2026-06-26 | Live refresh vs the 2026-06-25 baseline snapshot. **No state, milestone, title, or list changes** — watchlist holds at 16 dependencies (10 open · 3 closed · 3 merged). 1 item had new upstream activity; bumped Updated cell: `WordPress/gutenberg#77994` → 2026-06-25. |
| 2026-06-25 | Live refresh vs the 2026-06-20 baseline snapshot. **No state, milestone, or list changes** — watchlist holds at 16 dependencies (10 open · 3 closed · 3 merged). 1 title change: `WordPress/gutenberg#77230` retitled "Guidelines: Evolve CPT to enable support for skills, memory, and plans via taxonomy" → "Guidelines built on Knowledge in WordPress 7.1". 3 items had new upstream activity; bumped Updated cells: `#70710` → 2026-06-22, `#77230` → 2026-06-22, `#77994` → 2026-06-24. |
| 2026-06-20 | Live refresh vs the 2026-06-19 baseline snapshot. **No state, milestone, or title changes** — watchlist holds at 16 dependencies (10 open · 3 closed · 3 merged). 2 items had new upstream activity (`WordPress/gutenberg#74234`, `#70710`); bumped #74234's Updated cell to 2026-06-19. |
| 2026-06-19 | Initial Gutenberg + abilities-api dependency watchlist. Added 16 tracked dependencies and wired the refresh script to fetch, diff, render, and snapshot them separately from Project #240 counts. |
