# WordPress AI — Planning & Roadmap (Living Document)

> **Source board:** [github.com/orgs/WordPress/projects/240](https://github.com/orgs/WordPress/projects/240) — *"WordPress AI Roadmap"*
> *"A project board to provide oversight into the WordPress AI plugin."*
>
> | | |
> |---|---|
> | **Data snapshot** | 2026-09-23 04:28 UTC (latest board-item activity: 2026-09-22 16:42 UTC — issue #1054; the project record itself last changed at 2026-09-22 16:42 UTC) |
> | **Items captured** | 309 (full board, via GraphQL Projects API) — **up 19 from 290** |
> | **Plugin** | [`WordPress/ai`](https://github.com/WordPress/ai) — the official "AI" Showcase Plugin on WordPress.org |
> | **Latest shipped** | **v1.3.0** (shipped 2026-08-18 21:04 UTC) — still the latest plugin release, **36 days on**, with **no 1.4.0 release issue**. The 1.3.0 repository milestone stays closed at 71 items. The **board** lane is now **13 cards (12 Done + Google-provider #23)**, down from 16 at the previous snapshot and from 19 on release night. |
> | **In active development** | **v1.4.0** (13 Done / 12 open; 25 carded; no release issue or date) · **v1.5.0** (1 Done / 8 open; the Done card is `php-ai-client#284`, the client's own 1.5.0 release) · **v1.6.0** (3 open, new on the board) · **Backlog:** Future Release (49 open) |
> | **Maintained by** | _(you)_ — see [§10 How to refresh](#10-how-to-refresh-this-document) to regenerate the data |

**How to read this doc:** [§1 Composition](#1-board-composition) · [§2 Releases](#2-release-cadence) · [§3 Product model](#3-product-architecture) · [§4 Shipped](#4-shipped-done) · [§5 In progress](#5-in-progress) · [§6 Roadmap bets](#6-planned-roadmap--strategic-bets) · [§7 Undecided/blocked](#7-unplanned--undecided--blocked) · [§8 Strategic read & risks](#8-strategic-read--risks) · [§9 Full open-issue tracker](#9-appendix--full-open-issue-tracker-65) · [§10 Refresh](#10-how-to-refresh-this-document) · [§11 Changelog](#11-changelog)

> **📚 Companion documents (4-doc set):**
> 1. **This file** — strategy, board composition, release cadence, and the at-a-glance open-issue tracker.
> 2. **[`wordpress-ai-open-issues.md`](./wordpress-ai-open-issues.md)** — deep per-issue dossiers, grouped by status. *(Current to 2026-09-23: all 65 non-Done issue cards, plus removed-board references #84 and `php-ai-client#242`, and the recently board-Done set retained for reference.)*
> 3. **[`wordpress-ai-planned-work.md`](./wordpress-ai-planned-work.md)** — the release-ordered delivery plan for all 86 non-Done cards, including 20 open board-tracked PRs and stale merged PR #484.
> 4. **[`wordpress-ai-cross-repo-dependencies.md`](./wordpress-ai-cross-repo-dependencies.md)** — the 16-item Gutenberg + abilities-api dependency watchlist (11 + 5; 9 open / 4 closed / 3 merged, zero drift again), plus full PR/issue/release censuses for all four tracked repositories.

### Status legend (board's own taxonomy)
`Triage` → unsorted/new · `In discussion / Needs decision` → debated, **not** committed · `Backlog` / `To do` → planned · `In progress` → being built · `Needs review` → review-ready · `Done` → shipped/merged

---

## Executive summary

Project #240 remains the operational tracker for the [`WordPress/ai`](https://github.com/WordPress/ai) Showcase Plugin (307 of 309 cards; the others are Google-provider issue #23 and `php-ai-client#284`, the client's 1.5.0 release card, now Done). The plugin continues to organize user-facing AI capabilities as toggleable experiments over shared Connectors, AI Client, Abilities API, MCP, and request-log infrastructure.

**The board grew 290 → 309** = 236 PRs + **73** issues, Done **216 → 223**, non-Done **74 → 86**. That is 29 cards added, 10 removed, 8 status moves, 19 milestone moves, and 3 open-to-closed transitions. Unlike the previous window, the board was edited: work was rescheduled out of 1.4.0 into 1.5.0, 1.6.0, and Future Release, and two new lanes now hold cards.

**Nothing new has shipped from the plugin, and a lot has merged into `develop`.** v1.3.0 (2026-08-18) is still the latest tag, 36 days later, and there is still no 1.4.0 release issue. GitHub search counts **42 `WordPress/ai` pulls merged since the 2026-08-26 snapshot**. The substantive ones are the embedding programme (#975, #976, #993), Content Translation follow-through (#977, #986, #987), comment-value scoring (#681), ability renames (#1002), a new default image model (#1023), decorative-image alt text (#1031), and a run of test and dependency PRs under the 1.4.0 milestone. The repository's `1.4.0` milestone is now **83 items (63 closed / 20 open)**. Its 20 open items are all represented on the board, either as cards or through a closing link. The historical hole is the closed side: most of what completed under 1.4.0 was never a card.

**The embedding gate merged, and the plugin followed it the same afternoon.** [`php-ai-client#274`](https://github.com/WordPress/php-ai-client/pull/274) merged **2026-08-31 at 16:44 UTC**. [**#975**](https://github.com/WordPress/ai/pull/975) merged at **20:18** the same day, bringing that required-model API into the plugin and turning `SDK_Overlay` back on. [**#976**](https://github.com/WordPress/ai/pull/976) merged **2026-09-02** — the portable `wpai_embeddings` store. [**#993**](https://github.com/WordPress/ai/pull/993) (dkotter, +1,616/−36) added similarity calculations on **2026-09-14**. All three plugin PRs were uncarded. Tracking issue **#962** then moved **In progress → In discussion** and stayed on 1.4.0. The closed-upstream straggler `php-ai-client#242` was de-carded. Two later checklist items were filed as issues with **empty bodies** (#1036 background sync, #1037 docs). `php-ai-client` **1.5.0** shipped **2026-09-16** and includes #274.

**The AI Workspace is the new large object.** [**#1004**](https://github.com/WordPress/ai/pull/1004) (whyisjake, 2026-09-04, **+25,745/−33 across 91 files**) implements board issue **#282** — a full-screen admin conversation that can search and read content under the requesting user's own capabilities, proposes drafts instead of writing them, and streams when the provider can. #282 moved **Backlog → In progress**. The PR is not itself a card; it closes #282. It is BLOCKED with failing checks. Two follow-up issues already exist: **#1003** (admit tools from the Abilities API rather than a two-ability allowlist; PR #1014) and **#1006** (failure and replay policies, still Triage, unmilestoned).

**Coverage improved by finishing or linking, and one typo PR is the remaining miss.** Open PRs **33 → 41** = **20 direct-board-pr + 20 linked-board-issue + 0 routine + 0 off-board + 1 unexplained**. The previous unexplained set is gone: #975, #976, and #977 merged, and **#961 now has an authoritative `closing` reference to #923**. The new failure is [**#1057**](https://github.com/WordPress/ai/pull/1057) (ahirpravin, +2/−2, two documentation typos). **64 of 64 open `WordPress/ai` issues are carded.** Three fallback-parsed relationships remain, all on PRs that are already board cards (#494→#238 by title, #943→#891 by title, #992→#354 by body).

**The approval streak on the open queue broke, twice, and neither approval is mergeable.** **#459** (C2PA Monitor) went `CHANGES_REQUESTED` → **APPROVED**, with green checks, and is still BLOCKED. **#973** (the SEO-plugin cache fix for #970) also went to **APPROVED**, and its checks went SUCCESS → **FAILURE**. Everywhere else the queue is familiar: **29 BLOCKED / 10 DIRTY / 1 CLEAN / 1 UNSTABLE**, **21 failing checks**, 9 `CHANGES_REQUESTED`, 8 drafts. The one CLEAN pull is draft **#1026** (content write abilities through the REST API). **#935** (connector validity) is still green, still unapproved, and has now been review-ready since 2026-08-19.

**1.4.0 was unloaded.** Twelve non-Done cards remain there (11 issues + PR #888), down from 24. **#764** and **#858** moved to **1.5.0**, along with **#923** (In discussion → In progress; #961 is now its closing PR, +3,224 across 13 files) and **#338** (In progress → Needs review). **#660**, **#732**, and **#875** moved to a new **1.6.0** lane. **#683**, **#777**, **#789**, **#832**, **#933**, **#943**, and **#945** moved to Future Release. **#27** moved back from To do to In discussion. Google-provider **#23** moved **1.2.0 → 1.3.0**, so the plugin's 1.2.0 board lane is entirely Done and #23 is the one open card on 1.3.0.

**Two closed-unmerged PRs were marked Done, and one declined issue was too.** **#211** (Service Account) and **#760** (Chrome DevTools PoC) were closed unmerged, marked Done, and had their milestones cleared. **#1024** (Suggest Reply on the Edit Comment screen) was closed **`NOT_PLANNED`** the same minute **#1028** was closed unmerged, and the board card is Done. **#514** shipped — #681 merged 2026-09-03 — and the card was **removed from Needs review** rather than moved to Done. **#735**, the implementation of still-open **#689**, was closed unmerged.

**Upstream: the client released; the adapter merged its tooling and closed its drafts; abilities-api did not move.** `php-ai-client` **31 → 37** open PRs / **35 → 40** issues / **1.5.0** (2026-09-16). The release notes credit #275, #231, **#274**, and **#282**. That last one needs a careful reading: the PR that was open on 2026-08-26 as deferred tool loading **merged on 2026-09-10 retitled "Add generic annotations to function declarations"** (+141/−5 across 3 files). Issue **#281** (the actual deferred-loading proposal, the upstream answer to board **#21**) is **still open**. New **#283** (+1,198) is the citation DTO for still-open **#277**. `mcp-adapter` **17 → 16** PRs / **43 → 51** issues / **v0.6.1**. **#288** (deprecate bundled-dependency use) merged 2026-09-01, as did #302 and #303; #304 had merged 2026-08-26. Drafts **#260**, **#281**, **#287**, and **#300** were closed unmerged. **#299** (the `resources/read` 500) is still open; its checks are now green and it needs a rebase. A `ci-artifacts` prerelease was published 2026-08-27; it is not a product release. `abilities-api` is **14 / 8 / v0.4.0 for a sixth census**, archival issue #160 still untouched since 2026-02-04. The dependency watchlist is **16 (9 open / 4 closed / 3 merged) with an empty diff on every axis** — the third consecutive refresh with no change, and every `updatedAt` is still the 2026-08-17 value.

The forward roadmap still converges on four bets: (A) Abilities as the tool layer — #923/#961 are now an in-progress 1.5.0 pair with a real closing link, #1004 is the workspace that consumes abilities, and #945's `core/edit-content` was pushed to Future Release; (B) Connectors, where #933/#935 is still the review-ready fix nobody has approved, now Future Release rather than 1.4.0; (C) the editorial lifecycle, which kept landing small translation fixes (#980/#981 Done, #977 merged) while Show-template bugs #1052 and #1054 opened; and (D) conversational and semantic-search surfaces, where the embedding substrate is **back on in `develop`** and the workspace PR is the largest new artifact. The active board lane is **v1.4.0 (12 non-Done, no release issue, no date)**; **v1.5.0 holds 8 non-Done**; **v1.6.0 holds 3**; **Future Release holds 49**.

---

## 1. Board composition

| Dimension | Breakdown |
|---|---|
| **Total items** | **309** = 236 PRs + 73 issues *(290 → 309: +29 added, −10 removed)* |
| **By status** | Done **223** · In progress **35** · In discussion/Needs decision **16** · Needs review **10** · Triage **16** · Backlog **6** · To do **3** |
| **Open work** | **86 non-Done cards** = 65 issue cards + 21 PR cards. *One of those is a closed-state straggler:* #484 is merged but still board-Needs review. `php-ai-client#242` was de-carded. Live open work is **64 open `WordPress/ai` issues + Google-provider #23 + 20 open board PRs**. |
| **By repo** | `WordPress/ai` **307** · `WordPress/ai-provider-for-google` **1** (#23) · `WordPress/php-ai-client` **1** (#284, Done). `WordPress/abilities-api` #84 is not on Project #240. |
| **Cross-repo dependency scope** | Separate watchlist: **16** dependencies (9 open, 4 closed, 3 merged), tracked in [`wordpress-ai-cross-repo-dependencies.md`](./wordpress-ai-cross-repo-dependencies.md). **Empty diff on every axis, third consecutive refresh.** |
| **Full repository census** | Open PRs / open issues / latest release — `WordPress/ai`: **41 / 64 / 1.3.0** · `php-ai-client`: **37 / 40 / 1.5.0** · `mcp-adapter`: **16 / 51 / v0.6.1** · `abilities-api`: **14 / 8 / v0.4.0** *(pending archival, static on every axis for a sixth census)*. Project #240 coverage applies only to `WordPress/ai`, where **all 64 open issues are carded** — enforced, not observed — and **1 of 41 open PRs has no board representation** (#1057). |
| **"Team" field** | No Team values are returned in the current Projects API snapshot; use repo + labels/status for classification. |
| **"Priority" field** | Barely used; prioritization is expressed through Status + Milestone. |
| **Board views** | Prioritized backlog · AI plugin · Status board · Roadmap (timeline) · Bugs 🐛 · My items |

**Caveat for maintainers:** "Done" (**223**) is overwhelmingly merged PRs; the forward-looking signal lives in the **65 non-Done issue cards** and **21 non-Done PR cards**. The total rose by 19 while the plugin cut no release. Read Done as "the card was closed and has not yet been tidied away", never as "the capability is live". Standing mismatches: **#1024** is board-Done and closed `NOT_PLANNED`; **#211** and **#760** are board-Done and closed unmerged; **#514** shipped via #681 and was removed without a Done status; **#484** is merged and still Needs review; **#621** and dependabot **#922** remain Done though closed unmerged. **#892** is Done and, as of #975's merge on 2026-08-31, the overlay it described is loading again.

**Ten cards left the board.** Already-Done **#863**, **#866**, **#906**, and **#924** (1.3.0) plus **#940** and **#968** (1.4.0); open-at-removal **#224** (the WebMCP PR is still open and now closes #448), **#262** and **#514** (both closed upstream the day they were removed), and **`php-ai-client#242`**. **The 1.3.0 board lane therefore reads 13 cards — 12 Done plus Google-provider #23 — where it read 19 on release night.** The repository milestone still says 71 items. Board membership is not a stable identifier for historical analysis; the git-tracked snapshots in `.wp-ai-roadmap-snapshots/` are.

---

## 2. Release cadence

The plugin has shipped **19 releases**, most recently **v1.3.0 on 2026-08-18**. That is now **36 days** with no successor tag and **no 1.4.0 release issue** — past the window in which #924 was filed (eight days before 1.3.0). The board has meanwhile split the old 1.4.0 pile across **1.4.0, 1.5.0, and a new 1.6.0**. None of the three carries a due date. The cadence to date had been roughly two weeks per minor; **1.2.0 → 1.3.0 took 35 days**, and 1.3.0 → whatever is next is already longer. Upstream, `php-ai-client` **did** release: **1.5.0 on 2026-09-16**.

| Milestone | Board cards | State | Meaning |
|---|---:|---|---|
| 0.1.0 → 0.9.0 | 134 | 133 Done / 1 stale | Build-out history. #484 is merged but still board-Needs review under 0.9.0. |
| 1.0.0 | 3 | ✅ Done | Stability milestone aligned with WordPress 7.0 / the Abilities API. |
| 1.0.1 | 8 | ✅ Done | Prior patch release. |
| 1.0.2 | 3 | ✅ Done | Prior patch release (2026-06-16). Several already-Done cards were later removed from the board. |
| 1.1.0 | 11 | ✅ Done | Shipped 2026-07-01. All remaining cards are Done. |
| **1.2.0** | **8** | ✅ **8 Done** | **Shipped 2026-07-14.** Google-provider #23 left this lane for 1.3.0, so the plugin's 1.2.0 board lane is entirely Done. |
| **1.3.0** | **13** *(was 16)* | ✅ **12 Done / 1 open** | **Shipped 2026-08-18.** The lane closed 19/19 on release night and has been emptied twice since: three cards on 2026-08-26, four more this window (#863, #866, #906, #924). The one open card is **Google-provider #23**, moved in from 1.2.0. It is not unfinished plugin work. The repository milestone remains closed at **71 items**. |
| **1.4.0** | **25** *(was 30)* | 🚧 **13 Done / 12 open** | **The active lane, unloaded.** 12 non-Done = **11 issue cards + PR #888**. Cards moved out to 1.5.0, 1.6.0, and Future Release. The repository milestone now holds **83 items (63 closed / 20 open)**. All 20 open items are represented, as cards or through a closing link. The hole is historical: most of what already completed under 1.4.0 was never a card. Two issues remain undecided (#324, #741). **No date, and no release issue.** |
| **1.5.0** | **9** | 🚧 **1 Done / 8 open** | First cards. The Done card is **`php-ai-client#284`**, the client's own 1.5.0 release, so this column mixes repositories. Open: issues #923, #979, #338 and PRs #764, #992, #1041, #858, #1010. The repository milestone holds **11 open / 0 closed**, all represented (those cards plus linked #961, #1009, #929). |
| **1.6.0** | **3** | 🆕 **0 Done / 3 open** | New lane: #660 (Needs review), #732 (In progress), #875 (In progress). The repository milestone holds **6 open / 0 closed**, all represented (those three plus linked #887, #759, #757). |
| **Future Release** | **49** *(was 42)* | 📋 **0 Done / 49 open** | Long-range backlog: 35 issues + 14 PRs. Grew as 1.4.0 was unloaded (#683, #777, #789, #832, #933, #943, #945, and #971 in from unmilestoned) and new cards arrived. |
| _(no milestone)_ | **43** | 31 Done / 12 open | The twelve open cards are #1037 (embeddings docs), the five C2PA follow-ups **#953–#957**, and six new Triage issues (**#994, #1006, #1012, #1016, #1036, #1048**). #971 left this bucket for Future Release. |

*Sums to 309 cards. Non-Done by lane: 1.4.0 12 + 1.5.0 8 + 1.6.0 3 + Future Release 49 + 1.3.0 residual 1 (#23) + unmilestoned 12 + stale #484 = 86.*

---

## 3. Product architecture

The mental model that explains every item on the board:

**Experiments (user-facing, toggleable AI features):**
Title Generation · Excerpt Generation · Alt Text Generation · Image Generation · Content Summarization · Content Classification · Content Resizing · Comment Moderation · "Refine"/Editorial Updates (Notes) · Abilities Explorer · Content Translation · Slug Generation · and, in `develop` but not yet released, the AI Workspace

**Shared infrastructure:**
- **Connectors** — the screen where AI **provider plugins** register (Anthropic / Google / OpenAI by default).
- **Abilities API** (WP core) — the tool/capability layer; **the strategic keystone** (maps to MCP, WebMCP, Skills, Command Palette).
- **WP AI Client** (WP 7.0 core) — the in-core prompt/model client features call.
- **AI Request Logs** — observability (the dominant recent theme).
- **MCP** adapters — expose abilities to external agents.
- **Embeddings** — vendored from `php-ai-client` behind `SDK_Overlay`. Switched off on 2026-08-17 by #946; **switched back on in `develop` on 2026-08-31 by #975**, with storage (#976) and similarity (#993) merged behind it. Not in a plugin release yet.
- ~~**`AI_Service`** layer~~ — **deprecated, and the card is gone.** Introduced in 0.2.1 (#101) as shared internal routing that experiments would migrate onto (#233), it was never adopted. PR [#905](https://github.com/WordPress/ai/pull/905) **merged 2026-08-11**: the class and the `get_ai_service()` helper stay, marked deprecated, after dkotter noted both are public surface a third party could be calling. **#233 closed against it**, and **#233 was de-carded on 2026-08-26**. All experiments call `wp_ai_client_prompt()` directly.

Every roadmap item is a **new Experiment**, an **enhancement to one**, or **infrastructure/governance** beneath them.

---

## 4. Shipped (Done)

**Board-Done moved 216 → 223.** Ten cards arrived already Done, three existing cards were marked Done, and six already-Done cards were deleted. The plugin still has not shipped: the latest release is **v1.3.0 (2026-08-18 21:04 UTC)**, 36 days on, with no 1.4.0 release issue. GitHub search counts **42 pulls merged into `develop` since the 2026-08-26 snapshot**.

### What v1.3.0 contains

The release notes credit four new Experiments and a substantial infrastructure layer:

- **Content Translation** (#747) — Paragraph/Heading blocks and optionally the post title, translated in the editor.
- **Slug Generation** (#897, #932) — SEO-friendly permalink suggestions.
- **Custom Abilities** (#881) — the standalone-ability opt-in gate from #863: `ai/get-post-details`, `ai/get-post-terms`, `core/read-settings`, `core/read-users`, `core/read-content` now require the experiment be enabled. **A behavior break for anyone already calling them.**
- **Import/Export of non-sensitive AI settings** and **AI-specific Site Health tests** (#734), **uninstall data cleanup** (#692), **prompt-template extension points** (#770), the public **`WordPress\AI\log_ai_request()`** API (#914), the `wpai_` meta-key rename with migration (#867), and refreshed default models (#913).

**The embedding changelog entry is true again in `develop`, and still not in a release.** PR #946 switched off #892's `SDK_Overlay` on 2026-08-17. The published 1.3.0 notes kept the entry anyway, crediting both PRs. **#975 merged 2026-08-31 at 20:18 UTC** and loads the overlay again against `php-ai-client#274`'s required-model API. That corrective is in `develop` only. v1.3.0 on WordPress.org is unchanged.

### What reached Done, and what that status means

Three pre-existing cards became Done, and only one of them shipped:

- **#791** (Type Ahead loading cursor) closed **COMPLETED** on 2026-09-18 via merged **#1029**, and its milestone moved Future Release → 1.4.0.
- **#211** (Service Account) was **closed unmerged** on 2026-09-17, marked Done, and had its milestone cleared.
- **#760** (Chrome DevTools PoC) was **closed unmerged** on 2026-09-03, marked Done, and had its milestone cleared.

Ten cards arrived already Done. The ones that represent merged work: **#980** / **#981** (Content Translation guidelines and default languages, via #987 and #986), **#1021** (deactivation unit tests, #1022), **#1030** (decorative-image alt text for WP 7.1, #1031), **#1034** (accordion focus style, #1035), **#1039** (Update/actions), **#1049** (hide Suggest Reply in Quick Edit), **#978** (Playground preview), and **`php-ai-client#284`** (the client's 1.5.0 release card, filed under the board's 1.5.0 column). **#1024** (Suggest Reply on the Edit Comment screen) arrived Done and was closed **`NOT_PLANNED`** the same minute **#1028** was closed unmerged.

**#514 shipped and was not marked Done.** PR **#681** merged 2026-09-03, the issue closed COMPLETED the same minute, and the card was **removed from Needs review**.

### What merged without a card

The substantive uncarded merges, most of them repository-milestoned 1.4.0:

- **#975, #976, #993** — the embedding programme (re-enable, storage, similarity). #1005 (PSR EventDispatcher in the vendored `EmbeddingBuilder`) merged 2026-09-04.
- **#977** — Content Translation loading effect, merged 2026-08-26, hours after the previous snapshot.
- **#681** — comment value scoring, closing #514.
- **#1002** — rename content/users abilities and remove `ai/get-post-details`.
- **#1023** — default OpenAI image model `gpt-image-2.5-flare`.
- **#1001** — developer-tools icon, closing #995.
- A run of test, docs, DataViews, and dependency PRs (#1007, #988–#991, #1008, #1011, #1013, #1022, #1031, #1033, #1035, #1038, #1043–#1046, #1051, #1056, and dependabot).

Closed unmerged, besides #211 and #760: **#735** (the implementation of still-open #689), **#1028** (with #1024), **#984**, **#1050**, and dependabot block-library #1000 and #1047. GitHub search under-counts closed-unmerged pulls in this window; the list above is from the pull records, not from that search total.

### Board removals

**Ten cards left.** Already-Done **#863, #866, #906, #924** (1.3.0) and **#940, #968** (1.4.0). Open-at-removal: **#224** (WebMCP PR, still an open draft, now closing #448), **#262** (closed COMPLETED 2026-09-03 — the feature had landed in 0.9.0 — and removed while still In discussion), **#514** (shipped, removed from Needs review), and **`php-ai-client#242`** (the closed-upstream straggler, closed 2026-07-15). The 1.3.0 board lane now reads **13**.

The durable shipped foundation remains:

- Toggleable experiments over shared Connectors and the WordPress AI Client.
- Abilities Explorer and an expanding set of read/manage abilities, behind the Custom Abilities opt-in.
- MCP integration and request-log observability, with a public logging API.
- Type Ahead, Suggest Reply, Content Classification, generation, summarization, translation, slug suggestions, and editorial workflows.
- In `develop` only: an embedding substrate that loads again, and the start of an AI Workspace.

---

## 5. In progress

The active build is no longer one lane. **v1.4.0 holds 12 non-Done cards (11 issues + PR #888)**, **v1.5.0 holds 8**, **v1.6.0 holds 3**, and **Future Release holds 49**. None of the three numbered lanes has a date or a release issue. The repository's open milestone sets are fully represented: 1.4.0 is 20 open items, 1.5.0 is 11, 1.6.0 is 6. Future Release is not — the repository milestone is **54 open** against **49** board cards.

Clusters, by where the board put them:

- **1.4.0 — what is still aimed at the next plugin release:** C2PA detection (#421, PR #459 now **APPROVED** and still BLOCKED), the `Enable AI` toggle (#600, PR #985), role/user access (#736, PR #749, checks now failing), Markdown feeds (#845, PR #855, checks now green), request-log 4xx/5xx (#1032, PR #1040), summarization with Show template (#1054, PR #1055), Text to Speech (#888, now DIRTY), the SEO cache bug (#970, PR #973 **APPROVED** with failing checks), embeddings tracking (#962, In discussion, substrate merged), provider discovery (#27, back in discussion), admin flicker (#741), and collaborative Refine (#324).
- **1.5.0 — loaded for the first time:** agent users (#923, closing PR #961), Content Translation block coverage (#979, PR #1009), analytics (#338, PR #929, checks now failing), `core/manage-settings` (#764), `core/read-nav-menus` (#858), MCP Access (#992), background post-embedding sync (#1041), and agent-user docs (#1010, merge **UNSTABLE**).
- **1.6.0 — three cards moved out of 1.4.0 together:** connector-approval error copy (#660, PR #759), non-SDK request logging (#732, PR #757), internal links (#875, PR #887).
- **Future Release — where the large objects went:** the AI Workspace (#282 / #1004, and #1003 / #1014), `core/edit-content` (#945), native vector search (#683), semantic search (#844 / #891 / #943), Knowledge and Guidelines (#949, no longer a draft, checks now failing), content-write abilities (#1025, #1026 — #1026 is the queue's only **CLEAN** pull, and it is a draft), connector validity (#933 / #935, still green and unapproved), and governance RFC #971.
- **Unmilestoned:** embeddings docs (#1037, PR #1042) and background sync as an issue (#1036, empty body; #1041 does not close it), C2PA follow-ups #953–#957, onboarding (#994), workspace failure policy (#1006), Secrets API (#1012), Abilities Explorer on DataViews (#1016), and the Settings `admin_email` null bug (#1048).

**Eight cards changed status and nineteen changed milestone.** This window is the opposite of 2026-08-26, when both arrays were empty. In progress **25 → 35**, In discussion **19 → 16**, Needs review **9 → 10**, Triage **9 → 16**, Backlog **7 → 6**, To do **5 → 3**.

The repo has **41 open PRs**: **20** direct board PR cards, **20** linked to board issues by authoritative `closing` references, and **one unexplained** — **#1057** (documentation typos). **64 of 64 open `WordPress/ai` issues are carded.**

Readiness of those 41, after GitHub finished computing mergeability: **29 BLOCKED / 10 DIRTY / 1 CLEAN / 1 UNSTABLE**, **21 failing checks**, **2 APPROVED** (#459, #973), 9 `CHANGES_REQUESTED`, 8 drafts. The approving-review streak on the open queue broke. Neither approval is mergeable.

Upstream, without folding it into Project #240: **`php-ai-client` 31 → 37 open PRs / 35 → 40 issues / 1.5.0** (2026-09-16). **#274 merged.** **#282 merged retitled as generic annotations on function declarations**; deferred tool loading (**#281**) is still an open issue. **`mcp-adapter` 17 → 16 / 43 → 51 / v0.6.1**, with #288, #302, #303, and #304 merged and #299 now green and DIRTY. **`abilities-api` 14 / 8 / v0.4.0 for a sixth census.**

*(Full per-issue detail is in [§9](#9-appendix--full-open-issue-tracker-65).)*

---

## 6. Planned roadmap — strategic bets

Four converging directions. Milestone placement below is the 2026-09-23 board, not the August placement.

### A. Abilities API as the universal tool layer → Platform & Standards
The keystone bet: **`name + description + JSON Schema + implementation`** as the primitive that bridges WordPress into every agent standard.

- **#40 Core Abilities** *(Triage, Future Release)* — the foundational `core/*` ability set. Owners: gziolo, jorgefilipecosta. The write half, **#945** (`core/edit-content`), is carded Needs review under **Future Release**, DIRTY, checks green, still no approval. New draft PRs **#1025** and **#1026** add `core/content-create`, `core/content-update`, and `core/content-delete`, the second of them through the REST API. **#1026 is the only CLEAN open pull in the plugin, and it is a draft.**
- **#348 Unified AI Management Layer for Core** and **#354 Unified Abilities exposure controls** — still Future Release, still In discussion. **#992** (MCP Access experiment, andreilupu, In progress / **1.5.0**) references #354 from its body without a closing link, so the relationship is `fallback-body` on a card that is already on the board.
- **#736 Per-feature role/user access controls** *(In progress, 1.4.0; PR #749 by Infinite-Null — BLOCKED, CHANGES_REQUESTED, checks now FAILURE)* — the middle governance thread. Identity (#923/#961) moved to **1.5.0 In progress** with a real closing link. Usage governance (**#971**) is still Triage, now Future Release and assigned to WouterP0lman, and still names #732.
- **#923 Introduce agent users** *(In progress, 1.5.0; gziolo)* — the identity half has a closing PR. **[#961](https://github.com/WordPress/ai/pull/961)** (now **+3,224 across 13 files**) declares `Closes #923`. It is BLOCKED with failing checks. **#1010** (webmyc) adds integrator docs to the Agent Users README and is itself a board card under 1.5.0, Needs review, merge **UNSTABLE**, checks failing. The audit-and-attribution half is still deferred.
- **#21 Supporting thousands of abilities** *(In discussion, Future Release)* — **do not read `php-ai-client#282` as the answer shipping.** That PR was open on 2026-08-26 as a deferred-loading draft. It **merged on 2026-09-10 retitled "Add generic annotations to function declarations"** (+141/−5 across 3 files). Issue **#281**, the actual deferred-loading proposal, is **still open**. The client 1.5.0 notes credit #282; they are crediting the annotations change. New **#283** (+1,198, not a draft) is a citation DTO for still-open **#277**.
- **#430 Skills in a WordPress admin context** — still Future Release. **#949** (Knowledge and Guidelines) is still the plugin-side port of `gutenberg#77230`. It **left draft**, checks went SUCCESS → **FAILURE**, and it is still In progress under Future Release.
- **#448 WebMCP experiment** *(In discussion, Future Release; assignee jorgefilipecosta)* — PR **#224** was removed from the board. It is still an open draft, DIRTY, checks green, and it now **closes #448**, so it is `linked-board-issue` rather than a missing card. The spec risk is unchanged: WebMCP is an unstable W3C Community Group draft.
- **#37 MCP usage & request routing** — unchanged, Future Release.
- **`core/edit-content`** *(PR [#945](https://github.com/WordPress/ai/pull/945), Needs review / **Future Release**)* — moved off 1.4.0. Merge state went BLOCKED → **DIRTY**. Checks are still green. It still references #40 without a closing link. It has been review-ready, green, and unapproved since it was carded on 2026-08-23.
- Supporting: **#203** (Ability Table column hook, Future Release, no PR), **#32** (AI Playground, To do), **`core/manage-settings`** (#764, now **1.5.0**), **`core/read-nav-menus`** (#858, now **1.5.0**, Needs review), **#1002** merged uncarded and renamed the content/users abilities. The Custom Abilities toggle shipped in 1.3.0 and its card **#863 was de-carded this window**. Upstream `abilities-api` **#84** remains open and off the board. **#1016** (Triage, unmilestoned, jorgefilipecosta) proposes moving the Abilities Explorer onto DataViews.

### B. Connectors, Providers & Model Management

- **#502 Provider plugin discovery/curation/labeling** — still the strategic parent, still In discussion / Future Release.
- **#27 Surface additional provider plugins on Connectors** *(In discussion, 1.4.0)* — the 2026-08-23 move to To do was **reversed**. Still no PR and no assignee, fifteen comments, open since 2025-10-06.
- **#262 Provider-level model bucketing** — **closed COMPLETED on 2026-09-03 and removed from the board** while the card was still In discussion. jeffpaul's close says the feature landed in 0.9.0 via #486, with follow-up #995 closed by merged #1001. It is not open work.
- **#660** *(Needs review / **1.6.0**; PR #759, CHANGES_REQUESTED, checks FAILURE)* — clearer "blocked by Connector Approvals" error. Moved out of 1.4.0 with #732 and #875.
- **#933 Connector validity is capability-aware, not text-only** *(Needs review, **Future Release**; PR #935)* — moved off 1.4.0. Checks are still green, merge still BLOCKED, still no approving review. Review-ready since **2026-08-19**.
- **#940** — Done against the timeout bump (#947) on 2026-08-19, and **de-carded this window**. The core defect is mitigated, not routed around.
- **#1048** *(Triage, unmilestoned, board-new)* — `Settings::register()` calls `register_initial_settings()` during an admin form save, and `options.php` then stores `admin_email` as NULL. Every Settings › General save reports "not a valid email address." Present since #856 / 1.2.0. No PR.
- **#1012** *(Triage, unmilestoned)* — move Key Encryption onto `ericmann/secrets-api`. The vendored secrets manager was archived.

### C. Content & Editorial Experiments (the authoring lifecycle)

- **#297 Content Generation** and **#324 Refine → agentic editorial** *(#324 still In discussion / 1.4.0)* — the large bets did not gain implementation. #324 is one of two undecided issues left on 1.4.0.
- **#338 Analytics-aware content** *(Needs review, **1.5.0**; zeus2611)* — moved out of Future Release and out of In progress. PR **#929** checks went SUCCESS → **FAILURE**. Still no approval.
- **#625 Social Content Generation** *(In progress / Future Release)* — still no PR. The 2026-08-21 implementation demo on the issue is still the artifact.
- **#875 Internal-link suggestions** *(In progress / **1.6.0**)* — moved off 1.4.0. PR #887 is still CHANGES_REQUESTED with failing checks. Slug Generation (#876) shipped in 1.3.0 and was de-carded on 2026-08-26.
- **Content Translation kept landing, and two Show-template bugs opened.** Merged: **#977** (loading effect), **#987** (guidelines, board-Done **#980**), **#986** (more default languages, board-Done **#981**). Open: **#979** / **#1009** (more block types, In progress / 1.5.0) and draft **#1027** (per-block translation, In progress / Future Release). **#1054** (summarization not generated when Show template is on, In progress / 1.4.0, PR #1055) and **#1052** (Editorial Notes reviews template blocks instead of post content, In progress / Future Release, PR #1053) are the new failures of that same "Show template" path. #1054 is the latest card the board touched.
- **#949 Knowledge and Guidelines** — left draft, checks now failing, still Future Release. **#951** (AI block labels) is still Backlog.
- **#791** Type Ahead loading cursor — Done via #1029. **#514** comment value — shipped via #681 and removed from the board. **#1024** Suggest Reply on Edit Comment — board-Done, closed `NOT_PLANNED`. **#1049** hides Suggest Reply in Quick Edit and merged.
- **#1030** decorative-image alt text and **#1034** accordion focus both arrived Done under 1.4.0 via merged PRs the same window they were filed.

### D. Agentic / Chat / Site-Agent + Media

- **#142 Frontend chat**, **#189 Site Agent**, **#190 Site-wide insights** — still Future Release, still without implementation PRs. #142 is named as a downstream consumer of the embedding work, which now has a substrate.
- **#282 Admin "AI Workspace"** *(In progress, Future Release; karmatosed)* — this is the window's largest new artifact. **[#1004](https://github.com/WordPress/ai/pull/1004)** (whyisjake, 2026-09-04, **+25,745/−33 across 91 files**) closes #282. It is an `ai-workspace` experiment (Tools → AI Workspace, `manage_options`): each tool call goes through `execute_ability()`; `ai/search-content` and `ai/read-content-bodies` (at most five posts, capability-checked at execute time); drafts are proposed and confirmed, with no write ability; Anthropic SSE streaming with a non-streaming fallback; the editor handoff seeds identity, not the post body. The PR is BLOCKED with failing checks and no review. It is `linked-board-issue`, not itself a card. **#1003** (In progress / Future Release) and **#1014** let the tool surface come from ability declarations instead of a two-ability allowlist. **#1006** (Triage, unmilestoned) is the policy residue: a mid-stream failure currently discards text already read and pays for it twice.
- **#238 / #494** focus-aware crops and **#325** Media Editor integration — unchanged in status. #494 is still DIRTY with green checks, and its `#238` link is still `fallback-title`.
- **#425** alt-text button placement — still In discussion, still unimplemented after #885 closed unmerged.

**C2PA provenance.** **#421** is still In progress / 1.4.0. PR **#459** went `CHANGES_REQUESTED` → **APPROVED**, checks still green, merge still **BLOCKED**, and the PR is still not itself a card. Follow-ups **#953–#957** are still Triage and unmilestoned, including **#955**, blocked by the C2PA Conformance Program. The cluster is six open cards and one approval that cannot merge.

**Search/RAG: the substrate is back on, and the feature PRs were pushed out of 1.4.0.** #975, #976, and #993 merged uncarded. `generate_embeddings()` is no longer the `WP_Error` #946 left behind. **#962** moved to **In discussion** and stayed on 1.4.0. **#683** (native vector search) moved to **Future Release** and is still a draft, DIRTY, checks failing. **#943** moved to Future Release, Needs review, BLOCKED, checks failing. **#844 / #891** are still Future Release / In progress. **#1041** (background sync for post embeddings, theaminulai) is In progress / **1.5.0**, BLOCKED, checks failing, and it does not close **#1036**. **#1036** and **#1037** were filed with **empty bodies**; #1042 closes #1037.

**Extensibility.** **#1002** merged and renamed abilities. **#918** (`/llms.txt`) is still Triage / Future Release. **#1017** (Triage / Future Release) is AI-assisted custom block creation, pointing at Telex / `georgestephanis/ai-block-creator`.

---

## 7. Unplanned / undecided / blocked

- **16 In discussion cards, all issues.** #211 and #224 left the status (#211 Done and closed unmerged; #224 de-carded). **#923** left for In progress. **#27** came back from To do. **#962** came back from In progress after its first steps merged. Google **#23** is still here, now on milestone 1.3.0.
- **16 Triage issues.** The old nine are still here, except that **#971** is now milestoned Future Release and assigned. Seven were added: **#994** (onboarding), **#1006** (workspace failure policy), **#1012** (Secrets API), **#1016** (Abilities Explorer on DataViews), **#1017** (custom block creation), **#1036** (background sync, empty body), **#1048** (admin_email nulled on save). #953–#957 are still unmilestoned.
- **Blocked direction:** #425 still has no Media Editor surface, and #494 is still the blocked implementation. **#955** is still blocked by the Content Credentials certification mark. **#689** is blocked in a new way: its PR **#735 was closed unmerged**, and the issue is still In progress. **#971** is still blocked on the #732 decision, and #732 moved to 1.6.0 without that decision being taken.
- **Review-ready work is no longer four issues under 1.4.0.** Needs-review *issues*: **#970** (1.4.0, PR #973 APPROVED, checks failing), **#338** (1.5.0, PR #929 checks failing), **#660** (1.6.0, PR #759 CHANGES_REQUESTED), **#933** (Future Release, PR #935 still the green unapproved pull, review-ready since 2026-08-19). Needs-review *PR cards*: stale **#484**, **#888** (1.4.0, now DIRTY), **#858** and **#1010** (1.5.0; #1010 is UNSTABLE), **#943** and **#945** (Future Release). **#514** left Needs review by being removed after it shipped.
- **Board hygiene changed shape.** `php-ai-client#242` is gone. **#484** is still merged and Needs review. New mismatches: **#1024** Done and `NOT_PLANNED`; **#211** and **#760** Done and closed unmerged; **#514** shipped and not marked Done. **#621** and dependabot **#922** remain Done though closed unmerged. **#1036** and **#1037** have null bodies.
- **Roadmap-visibility gap: 4 of 33 → 1 of 41.** The previous unexplained set merged (#975, #976, #977) or gained a closing link (#961 → #923). The remaining miss is **#1057**, a two-line documentation typo fix. **64 of 64 open issues are carded.** The historical hole did not close: the 1.4.0 repository milestone is **83 items, 63 of them already closed**, and most of those closed items were never cards. Open items on 1.4.0, 1.5.0, and 1.6.0 are fully represented.
- **Key open decisions:** whether #923, #736, and #971 are one governance design; whether #281's deferred loading is how #21 gets answered, now that #282 shipped as annotations instead; where #945 sits relative to #40; whether #935 is approved; which of #683, #891, and #943 owns semantic search now that #976's storage layer merged under them; what 1.4.0, 1.5.0, and 1.6.0 are each for, given none has a date; and whether #1024's Done status should survive a `NOT_PLANNED` close.

---

## 8. Strategic read & risks

**Where it is heading:** discrete AI helpers are becoming a governed, agent-capable WordPress platform. v1.3.0 shipped the Custom Abilities gate. Since then `develop` has absorbed the embedding substrate, a workspace that consumes abilities, agent-user identity, and a month of editorial fixes, and none of that is in a plugin release. The board finally edited itself: 1.4.0 was unloaded into 1.5.0, 1.6.0, and Future Release.

**Watch-items / risks:**

1. **The embedding dependency risk discharged, and the integration risk is now in merged code.** `php-ai-client#274` merged 2026-08-31. #975, #976, and #993 merged behind it, all uncarded. #962 moved back to In discussion with an unchecked-looking history that no longer matches `develop`. #683, #891, and #943 are still open on top of a storage layer that has already merged, and all three were pushed to Future Release. The 1.3.0 changelog entry is true in `develop` and false on WordPress.org until the next tag.
2. **Governance is closer to sequenced, and still uncoordinated.** #923/#961 are an in-progress 1.5.0 pair with a closing link. #736 is still 1.4.0 with a PR whose checks regressed. #971 is Triage on Future Release and still names #732, which moved to 1.6.0 and back to In progress. #1004's workspace executes abilities as the current user and proposes drafts rather than writing, which is a fourth governance choice made in code.
3. **Three active lanes, no dates, no release issue.** 1.4.0 has 12 non-Done board cards and a repository milestone of 83. 1.5.0 and 1.6.0 were loaded by moving cards, not by scoping a release. v1.3.0 is 36 days old. The previous minor took 35 days. The client shipped 1.5.0 on 2026-09-16; the plugin did not ship anything.
4. **Compatibility regressions are accumulating in review.** #935 (speech/image connectors) has been green and unapproved since 2026-08-19 and was moved to Future Release. #970's cache fix was approved and its checks went red. #1048 (admin email nulled on save) is Triage with no milestone and no PR. #1052 and #1054 are Show-template bugs with PRs already at `CHANGES_REQUESTED`. Upstream, `mcp-adapter#299` (the `resources/read` 500) is now green and needs a rebase.
5. **Open-PR coverage improved because work finished or was linked, which is the metric working for once.** 4 of 33 → **1 of 41**, and the remaining miss is a typo PR. The milestone view is still the one that describes history: 1.4.0 closed 63 items, most of them never carded. Do not quote the open-PR number as the fraction of the release the board can see.
6. **Board status is not delivery status, and this window added cases.** #1024 is Done and `NOT_PLANNED`. #211 and #760 are Done and closed unmerged. #514 shipped and was removed instead of marked Done. #484 is still non-Done though merged. #892 is Done and, as of #975, its description is true again in `develop`.
7. **The board total rose while the plugin stood still.** 290 → 309, with 42 merges into `develop` and zero releases. Read `.board.added` / `.board.removed` and the milestone table. The 1.3.0 lane is 13 cards against a closed milestone of 71.
8. **Approvals reached the open queue and did not make anything mergeable.** #459 and #973 are APPROVED. #459 is BLOCKED with green checks. #973 is BLOCKED with failing checks. Everywhere else: 29 BLOCKED, 10 DIRTY, 1 CLEAN (draft #1026), 1 UNSTABLE (#1010), 21 failing checks, 9 `CHANGES_REQUESTED`, 8 drafts. #935 is still the cleanest test of whether the queue receives review, and it has waited since 2026-08-19.
9. **`php-ai-client` 1.5.0 is easy to misread.** The notes credit #282. That PR merged as **generic annotations**, not deferred tool loading. #281 is open. #277 (citations) is open, with #283 proposed against it. Treating the client release as "the ability-catalog problem is solved" would be wrong, and it would make #21 look closer to done than it is.
10. **The watchlist has been frozen since 2026-08-17.** Sixteen dependencies, 9 open / 4 closed / 3 merged, every `updatedAt` unchanged, a third consecutive empty diff. Abilities-api is a sixth census at 14 / 8 / v0.4.0. The live upstream dependency this month was `php-ai-client`, and it released.

---

## 9. Appendix — full open-issue tracker (65)

> Deep dossiers live in [`wordpress-ai-open-issues.md`](./wordpress-ai-open-issues.md). The current scope is **65 non-Done issue cards**: 64 open `WordPress/ai` issues and Google-provider issue #23. `php-ai-client#242` was de-carded. PR cards are tracked in [`wordpress-ai-planned-work.md`](./wordpress-ai-planned-work.md).

Grouped by current board status. Theme tags are editorial aids. Status and Milestone come from Project #240.

### In discussion / Needs decision (16)

| # | Milestone | Theme | Assignee | Summary |
|---|---|---|---|---|
| [#23](https://github.com/WordPress/ai-provider-for-google/issues/23) | 1.3.0 | Bug/Providers | — | [Bug]: Image Generation fails with "Unexpected Google API response: Missing the candidates[0].content key" *(Moved **1.2.0 → 1.3.0**. The plugin's 1.2.0 lane is entirely Done; this provider bug is the one open card on 1.3.0)* |
| [#27](https://github.com/WordPress/ai/issues/27) | 1.4.0 | Providers | — | Display additional AI provider plugins on Connectors page (alongside default Anthropic, Google, and OpenAI ones) *(Moved **back** To do → In discussion. Still 1.4.0, still no PR and no assignee)* |
| [#324](https://github.com/WordPress/ai/issues/324) | 1.4.0 | Content | — | Evolve Refine from Notes into collaborative and agentic editorial workflows *(Still undecided, still 1.4.0)* |
| [#741](https://github.com/WordPress/ai/issues/741) | 1.4.0 | Bug/Infra | prasadkarmalkar | AI Admin Pages Exhibit Visible Flicker During Initial Render *(Still undecided, still 1.4.0)* |
| [#962](https://github.com/WordPress/ai/issues/962) | 1.4.0 | Platform/Embeddings | dkotter | Tracking: Implement Embedding Support *(Moved **In progress → In discussion** after #975, #976, and #993 merged. Substrate is back on in `develop`)* |
| [#21](https://github.com/WordPress/ai/issues/21) | Future Release | Platform | — | How to best support hundreds or thousands of abilities |
| [#37](https://github.com/WordPress/ai/issues/37) | Future Release | Platform | — | MCP usage across features and request routing |
| [#47](https://github.com/WordPress/ai/issues/47) | Future Release | Community | — | Low-/no-tech educational content |
| [#90](https://github.com/WordPress/ai/issues/90) | Future Release | Content | — | Clarify and consolidate Title Generation options in UI and Experiment settings |
| [#348](https://github.com/WordPress/ai/issues/348) | Future Release | Platform | — | Feature Request: Unified AI Management Layer for WordPress Core |
| [#354](https://github.com/WordPress/ai/issues/354) | Future Release | Platform | — | Unifiied Abilities exposure controls |
| [#425](https://github.com/WordPress/ai/issues/425) | Future Release | Agentic/Media | — | Update placement of Alt Text generation buttons |
| [#430](https://github.com/WordPress/ai/issues/430) | Future Release | Platform | — | Skills in a WordPress admin context |
| [#448](https://github.com/WordPress/ai/issues/448) | Future Release | Platform | jorgefilipecosta | Add WebMCP experiment *(Assignee **jorgefilipecosta**. PR #224 was de-carded and now closes this issue (draft, DIRTY, checks green))* |
| [#502](https://github.com/WordPress/ai/issues/502) | Future Release | Providers | — | Define how AI provider plugins are discovered, labeled, and surfaced in Connectors |
| [#643](https://github.com/WordPress/ai/issues/643) | Future Release | Bug | — | "AI" plugin 1.0.1 – Connectors and AI settings pages load blank (JavaScript error) on WordPress 7.0 |

### In progress (20)

| # | Milestone | Theme | Assignee | Summary |
|---|---|---|---|---|
| [#421](https://github.com/WordPress/ai/issues/421) | 1.4.0 | Agentic/Media | — | WordPress should detect C2PA manifests on upload *(Still 1.4.0. PR #459 is now **APPROVED** with green checks and still BLOCKED)* |
| [#600](https://github.com/WordPress/ai/issues/600) | 1.4.0 | Bug/Infra | — | Remove `Enable AI` header toggle, allow feature/experiment toggles (and group toggles) to control plugin functionality *(Moved **To do → In progress**. PR #985 (wprashed) is CHANGES_REQUESTED, checks green, BLOCKED. Gained `Good first issue`)* |
| [#736](https://github.com/WordPress/ai/issues/736) | 1.4.0 | Platform | — | Expose role/user access controls per feature/experiment *(PR #749 is CHANGES_REQUESTED, checks **FAILURE** (was SUCCESS), merge BLOCKED)* |
| [#845](https://github.com/WordPress/ai/issues/845) | 1.4.0 | Infra | dkotter | New Experiment: Markdown feeds (powered by `html-to-md`) *(PR #855 went DIRTY → **BLOCKED**, checks FAILURE → **SUCCESS**)* |
| [#1032](https://github.com/WordPress/ai/issues/1032) | 1.4.0 | Bug/Infra | — | AI Request Logs: HTTP error responses (4xx/5xx) are recorded as "success" *(**Board-new.** PR #1040 (theaminulai) closes it: BLOCKED, checks green)* |
| [#1054](https://github.com/WordPress/ai/issues/1054) | 1.4.0 | Content | — | Content Summarization: Summary is not generated when Show template is enabled *(**Board-new.** Latest board touch (2026-09-22). PR #1055 (yogeshbhutkar) is CHANGES_REQUESTED, checks green, BLOCKED)* |
| [#923](https://github.com/WordPress/ai/issues/923) | 1.5.0 | Platform | gziolo | Introduce agent users to give AI agents an auditable identity *(Moved **In discussion → In progress** and Future Release → **1.5.0**. #961 is now an authoritative `closing` link (+3,224 / 13 files))* |
| [#979](https://github.com/WordPress/ai/issues/979) | 1.5.0 | Content | — | Increase the types of blocks Content Translation supports *(**Board-new.** PR #1009 (dugyen) closes it: BLOCKED, checks green)* |
| [#732](https://github.com/WordPress/ai/issues/732) | 1.6.0 | Bug/Infra | — | AI Request Logging only captures providers that use the SDK HTTP transporter; sidecar/custom-transport providers are invisible *(Moved **Needs review → In progress** and **1.4.0 → 1.6.0**. PR #757 still CHANGES_REQUESTED / FAILURE. Still the prerequisite named by #971)* |
| [#875](https://github.com/WordPress/ai/issues/875) | 1.6.0 | Content | Infinite-Null | New Experiment: Suggest internal links within post content *(Moved **1.4.0 → 1.6.0**. PR #887 still CHANGES_REQUESTED / FAILURE)* |
| [#203](https://github.com/WordPress/ai/issues/203) | Future Release | Platform | — | Add extensibility hook for custom Ability Table columns |
| [#238](https://github.com/WordPress/ai/issues/238) | Future Release | Agentic/Media | TylerB24890 | Add focus-aware crop suggestions |
| [#282](https://github.com/WordPress/ai/issues/282) | Future Release | Agentic/Media | karmatosed | Chat experiment: Integration outside the editor and outside single-task AI use *(Moved **Backlog → In progress** behind #1004 (+25,745/−33, 91 files, BLOCKED, checks FAILURE))* |
| [#325](https://github.com/WordPress/ai/issues/325) | Future Release | Agentic/Media | TylerB24890 | Integrate media features and experiments with Gutenberg's experimental Media Editor |
| [#625](https://github.com/WordPress/ai/issues/625) | Future Release | Content | Malayt04 | New Experiment: Social Content Generation for platform-specific social posts |
| [#689](https://github.com/WordPress/ai/issues/689) | Future Release | Infra | i-anubhav-anand | Add a user-facing control for automatic log cleanup *(Implementing PR #735 was **closed unmerged** 2026-09-03. No open PR remains)* |
| [#844](https://github.com/WordPress/ai/issues/844) | Future Release | Agentic/Media | priyanshuhaldar007 | New Experiment: Semantic search in wp admin |
| [#1003](https://github.com/WordPress/ai/issues/1003) | Future Release | Agentic/Media | whyisjake | Let the AI Workspace's tool surface come from the Abilities API *(**Board-new.** PR #1014 (whyisjake) closes it: admit tools by declaration. BLOCKED, checks FAILURE)* |
| [#1052](https://github.com/WordPress/ai/issues/1052) | Future Release | Content | — | Editorial Notes: Only reviews template blocks instead of post content when "Show template" is enabled *(**Board-new.** PR #1053 (Infinite-Null) closes it: CHANGES_REQUESTED, checks green, BLOCKED. The PR's repo milestone is 1.4.0; the board card is Future Release)* |
| [#1037](https://github.com/WordPress/ai/issues/1037) | — | Platform/Embeddings | — | Document the architecture and developer-facing APIs for Embeddings support *(**Board-new, body empty.** Closing PR #1042 (theaminulai): BLOCKED, checks green)* |

### Backlog (6)

| # | Milestone | Theme | Assignee | Summary |
|---|---|---|---|---|
| [#142](https://github.com/WordPress/ai/issues/142) | Future Release | Agentic/Media | — | Frontend chat agent powered by site content |
| [#186](https://github.com/WordPress/ai/issues/186) | Future Release | Content | — | Add tone adjustment controls for AI-generated content |
| [#188](https://github.com/WordPress/ai/issues/188) | Future Release | Content | — | Add persona-driven content generation experiments |
| [#189](https://github.com/WordPress/ai/issues/189) | Future Release | Agentic/Media | — | Explore an admin Site Agent for executing WordPress actions |
| [#297](https://github.com/WordPress/ai/issues/297) | Future Release | Content | karmatosed | New experiment: Content Generation |
| [#951](https://github.com/WordPress/ai/issues/951) | Future Release | Content | — | New Experiment: Generate AI block labels in List View |

### To do (3)

| # | Milestone | Theme | Assignee | Summary |
|---|---|---|---|---|
| [#32](https://github.com/WordPress/ai/issues/32) | Future Release | Infra | — | Add AI Playground interface (prompt testing & debug tools) |
| [#190](https://github.com/WordPress/ai/issues/190) | Future Release | Agentic/Media | yogeshbhutkar | Add site-wide AI-powered content insights |
| [#339](https://github.com/WordPress/ai/issues/339) | Future Release | Bug | — | AI 0.6 + WP7RC1 + Gutenberg 22.7.1 : can't keep connection alive within the AI plugin |

### Triage (16)

| # | Milestone | Theme | Assignee | Summary |
|---|---|---|---|---|
| [#40](https://github.com/WordPress/ai/issues/40) | Future Release | Platform | gziolo, jorgefilipecosta | WordPress Core Abilities *(Still Triage. Its write half #945 moved to Future Release and is DIRTY with green checks)* |
| [#890](https://github.com/WordPress/ai/issues/890) | Future Release | UI/Mobile | — | Add mobile right sidebar display component |
| [#918](https://github.com/WordPress/ai/issues/918) | Future Release | Platform | — | Feature: AI-generated llms.txt - a curated, LLM-friendly site index served at /llms.txt |
| [#971](https://github.com/WordPress/ai/issues/971) | Future Release | Platform/Governance | WouterP0lman | RFC: AI governance experiment for role-based usage controls *(Still Triage, now milestoned **Future Release** and assigned **WouterP0lman**. Still names #732)* |
| [#1017](https://github.com/WordPress/ai/issues/1017) | Future Release | Content | — | New Experiment: AI-assisted custom block creation *(**Board-new.** AI-assisted custom block creation (Telex / georgestephanis/ai-block-creator))* |
| [#953](https://github.com/WordPress/ai/issues/953) | — | Agentic/Media | — | C2PA Monitor: JUMBF box reader and CBOR decoder for c2pa.decoded |
| [#954](https://github.com/WordPress/ai/issues/954) | — | Agentic/Media | — | C2PA Monitor: In-browser cryptographic verification via @contentauth/sdk |
| [#955](https://github.com/WordPress/ai/issues/955) | — | Agentic/Media | — | C2PA Monitor: Media Library grid view indicator |
| [#956](https://github.com/WordPress/ai/issues/956) | — | Agentic/Media | — | C2PA Monitor: Preserve C2PA manifests through WordPress GD/Imagick subsize pipeline |
| [#957](https://github.com/WordPress/ai/issues/957) | — | Agentic/Media | — | C2PA Monitor: Pre-fill CAI Verify tool with publicly reachable attachment URL (?source=) |
| [#994](https://github.com/WordPress/ai/issues/994) | — | Community | — | Improving onboarding *(**Board-new.** No comments)* |
| [#1006](https://github.com/WordPress/ai/issues/1006) | — | Agentic/Media | — | AI Workspace: the failure and replay stories need policies, not patches *(**Board-new.** Failure and replay policies left over from the #1004 review. Empty body of policy, no PR, no comments)* |
| [#1012](https://github.com/WordPress/ai/issues/1012) | — | Infra | — | Move the Key Encryption experiment onto the new Secrets API feature plugin *(**Board-new.** Key Encryption onto `ericmann/secrets-api` after vendored `displace-secrets-manager` was archived)* |
| [#1016](https://github.com/WordPress/ai/issues/1016) | — | Platform | jorgefilipecosta | Move the Abilities Explorer to DataViews *(**Board-new.** Assigned jorgefilipecosta)* |
| [#1036](https://github.com/WordPress/ai/issues/1036) | — | Platform/Embeddings | — | Add background synchronization and large-scale processing *(**Board-new, body empty.** PR #1041 does **not** close it)* |
| [#1048](https://github.com/WordPress/ai/issues/1048) | — | Bug | — | Settings::register() calls register_initial_settings() on admin form saves; options.php then saves admin_email as NULL and every Settings › General save shows "not a valid email address" *(**Board-new.** `Settings::register()` nulls `admin_email` on Settings › General save, since #856 / 1.2.0. No PR)* |

### Needs review (4)

| # | Milestone | Theme | Assignee | Summary |
|---|---|---|---|---|
| [#970](https://github.com/WordPress/ai/issues/970) | 1.4.0 | Bug | hbhalodia | Meta Description: SEO plugin detection cache never expires and skips the "no plugin" case *(Moved **In progress → Needs review**. PR #973 is **APPROVED** and its checks went SUCCESS → **FAILURE**)* |
| [#338](https://github.com/WordPress/ai/issues/338) | 1.5.0 | Content | zeus2611 | New Experiments: Analytics-aware content and amplification recommendations *(Moved **In progress → Needs review** and Future Release → **1.5.0**. PR #929 checks SUCCESS → **FAILURE**, still no approval)* |
| [#660](https://github.com/WordPress/ai/issues/660) | 1.6.0 | Providers | — | UX: Ambiguous error message in editor when a provider is blocked by Connector Approvals *(Moved **1.4.0 → 1.6.0**. PR #759 still CHANGES_REQUESTED / FAILURE)* |
| [#933](https://github.com/WordPress/ai/issues/933) | Future Release | Bug/Providers | whyisjake | Connector validity check assumes text generation, so speech-only and image-only connectors report as invalid *(Moved **1.4.0 → Future Release**. PR #935 still green, still unapproved, review-ready since 2026-08-19)* |

**Removed-board reference:** [`WordPress/abilities-api#84`](https://github.com/WordPress/abilities-api/issues/84) remains open upstream under milestone Later and is not counted in Project #240. **De-carded this window:** [#863](https://github.com/WordPress/ai/issues/863), [#866](https://github.com/WordPress/ai/issues/866), [#906](https://github.com/WordPress/ai/issues/906), [#924](https://github.com/WordPress/ai/issues/924) (1.3.0, all Done), [#940](https://github.com/WordPress/ai/issues/940) and [#968](https://github.com/WordPress/ai/issues/968) (1.4.0, Done), [#224](https://github.com/WordPress/ai/pull/224) (still an open draft, now closing #448), [#262](https://github.com/WordPress/ai/issues/262) and [#514](https://github.com/WordPress/ai/issues/514) (both closed upstream the day they left), and [`php-ai-client#242`](https://github.com/WordPress/php-ai-client/issues/242).

---

## 10. How to refresh this document

> ⚙️ **Automated path:** [`wp-ai-roadmap-refresh.sh`](./wp-ai-roadmap-refresh.sh) does everything below in one command — re-pulls the board, diffs against the last snapshot (added / newly-Done / merged / status & milestone moves / removed), fetches the full PR/release census for all repositories declared through [`wp-ai-roadmap-repositories.json`](./wp-ai-roadmap-repositories.json), refreshes the curated dependency watchlist, and can append a changelog row (`--update-changelog`). Run with no args for a read-only report; add `--save` to roll all board, repository, release, and dependency baselines forward. The manual recipe below is what it automates.
>
> **Normal vs. strict refresh.** The default (normal) refresh is *warning-only*: any data-quality or coverage problem — an unreachable dependency, a substantive open PR with no board representation — is reported in the output (`.validation` in JSON, stderr lines otherwise) but never blocks the board report, and the run exits `0`. `./wp-ai-roadmap-refresh.sh --strict [--json]` runs the same read-only pipeline as an **audit**: it emits the complete report first, then exits `2` when any validation error remains (exit `1` is reserved for operational failures that prevented a report at all). A strict failure also suppresses `--save` and `--update-changelog` ("persistence skipped"), so a red audit never rolls the baseline forward.
>
> Strict is a **scheduled visibility audit, not a merge gate**: whether every substantive `WordPress/ai` PR is linked to Project #240 depends on upstream contributor behavior, so a persistent red result means *unresolved roadmap visibility* (work in flight that the board doesn't show), not a broken tracker. Point-in-time coverage: **41 open PRs** = 20 direct board PR cards + 20 linked to board issues (**all 20 via authoritative `closing` references**) + 0 routine dependency PRs + 0 linked-off-board + **1 unexplained (#1057)**. Three fallback-parsed relationships remain, all on PRs that are already board cards: #494 → #238 by title, #943 → #891 by title, and #992 → #354 by body. The open-PR number improved this window because #975, #976, and #977 merged and #961 gained a closing link to #923. It still counts only PRs that are open. The stable historical measure is milestone-level: the repository's `1.4.0` milestone holds **83 items (63 closed / 20 open)**, and the open 20 are fully represented while most of the closed 63 were never cards. The issue-side audit has no such gap — every open `WordPress/ai` issue is checked against the board on every run (**64 of 64 carded**).
>
> **Cold-query caveat, now handled by the script.** GitHub computes PR mergeability lazily, so a first census can answer `mergeStateStatus: "UNKNOWN"` for PRs nothing has touched — the 2026-08-10 run reported 27 of 36 that way. `fetch_pr_census()` now re-asks once and keeps whichever answer knows more, warns (`pr-mergestate-unknown`, never an error) if any PR stays cold, and the PR diff ignores transitions into or out of `UNKNOWN`. Without that, one cold run reports every open PR as newly unreadable *and* persists the sentinel into the baseline, so the next window reports them all again in reverse.

**Prerequisite:** a `gh` login whose token has the `read:project` scope (fine-grained PATs scoped to a personal account **cannot** read the WordPress org project — even though it's public; use a classic-token login):

```bash
gh auth refresh -h github.com -s read:project
```

**1. Pull the full board** (paginated GraphQL → raw JSON). Save this query as `proj240.graphql`:

```graphql
query($org: String!, $number: Int!, $endCursor: String) {
  organization(login: $org) {
    projectV2(number: $number) {
      title shortDescription url
      items(first: 100, after: $endCursor) {
        totalCount pageInfo { hasNextPage endCursor }
        nodes {
          type
          fieldValues(first: 25) { nodes { __typename
            ... on ProjectV2ItemFieldSingleSelectValue { name field { ... on ProjectV2FieldCommon { name } } }
            ... on ProjectV2ItemFieldTextValue   { text field { ... on ProjectV2FieldCommon { name } } }
            ... on ProjectV2ItemFieldDateValue   { date field { ... on ProjectV2FieldCommon { name } } }
            ... on ProjectV2ItemFieldNumberValue { number field { ... on ProjectV2FieldCommon { name } } }
            ... on ProjectV2ItemFieldIterationValue { title startDate field { ... on ProjectV2FieldCommon { name } } } } }
          content { __typename
            ... on Issue { number title url state stateReason repository { nameWithOwner }
              labels(first:20){nodes{name}} assignees(first:10){nodes{login}} milestone{title}
              createdAt updatedAt closedAt comments{totalCount} }
            ... on PullRequest { number title url state isDraft repository { nameWithOwner }
              labels(first:20){nodes{name}} assignees(first:10){nodes{login}} milestone{title}
              createdAt updatedAt closedAt mergedAt }
            ... on DraftIssue { title assignees(first:10){nodes{login}} } } } } } } }
```

```bash
gh api graphql --paginate -f query="$(cat proj240.graphql)" -F org=WordPress -F number=240 > proj240_raw.json
```

**2. Normalize to flat items** (flattens custom fields into a `fields` object):

```bash
jq -s '[ .[].data.organization.projectV2.items.nodes[] ] | map({
  type:.type, itype:.content.__typename, number:.content.number, title:.content.title,
  url:.content.url, repo:.content.repository.nameWithOwner, state:.content.state,
  mergedAt:.content.mergedAt, milestone:.content.milestone.title,
  labels:[.content.labels.nodes[]?.name], assignees:[.content.assignees.nodes[]?.login],
  fields:( [ .fieldValues.nodes[] | select(.field.name!=null) |
    {key:.field.name, value:(.text // .name // (.date|tostring) // (.number|tostring) // .title)} ] | from_entries ) })' \
  proj240_raw.json > proj240_items.json
```

**3. Re-derive the key views:**

```bash
# Status distribution
jq -r 'group_by(.fields.Status)[] | "\(length)\t\(.[0].fields.Status)"' proj240_items.json
# All open issues (the roadmap), by status + milestone
jq -r '[.[]|select(.itype=="Issue" and .fields.Status!="Done")] | sort_by(.fields.Status,(.number)) | .[] |
  "[\(.fields.Status)] \(.milestone//"—") #\(.number) \(.title)"' proj240_items.json
# Milestone × Done/Open
jq -r 'group_by(.milestone)[] | "\(.[0].milestone//"(none)")\tTot:\(length)\tDone:\([.[]|select(.fields.Status=="Done")]|length)"' proj240_items.json
```

When refreshing: update the **Data snapshot** date in the header, re-check the §2 milestone table and §9 tracker, and add a §11 changelog entry.

---

## 11. Changelog

| Date | Change |
|---|---|
| 2026-09-23 | **Live refresh vs the 2026-08-26 09:19 UTC baseline — the board was edited, the embedding substrate merged, and the plugin still has not shipped.** Board **290 → 309** = 236 PRs + **73** issues; Done **216 → 223**; non-Done **74 → 86** = **65 issue cards + 21 PR cards** (20 open + stale merged #484). **29 added, 10 removed, 3 newly Done, 0 newly merged, 8 status, 19 milestone, 3 state.** v1.3.0 (2026-08-18) is still the latest plugin tag, **36 days** on, with **no 1.4.0 release issue**. GitHub search counts **42 pulls merged** since the previous snapshot. **The embedding gate merged and the plugin followed the same afternoon:** `php-ai-client#274` merged 2026-08-31 16:44 UTC; **#975** merged 20:18 the same day and turned `SDK_Overlay` back on; **#976** merged 2026-09-02; **#993** (similarity, +1,616/−36) merged 2026-09-14. All three plugin PRs were uncarded. Tracking issue **#962** moved In progress → In discussion and stayed on 1.4.0. **`php-ai-client` 1.5.0 shipped 2026-09-16.** Its notes credit #282, which **merged retitled as generic annotations on function declarations** (+141/−5); deferred tool loading (**#281**, the upstream answer to board **#21**) is **still open**. **Lanes:** 1.2.0 is **8 Done** after Google **#23** moved to 1.3.0; 1.3.0 is **13** (12 Done + #23) after #863/#866/#906/#924 were de-carded; **1.4.0 30 → 25** (13 Done / 12 open); **1.5.0 0 → 9** (the Done card is `php-ai-client#284`); **1.6.0 is new with 3 open** (#660, #732, #875); Future Release **42 → 49**; unmilestoned **43** (31 Done / 12 open). *Sums to 309.* Status: In progress **25 → 35**, In discussion **19 → 16**, Needs review **9 → 10**, Triage **9 → 16**, Backlog **7 → 6**, To do **5 → 3**. **#27** went back To do → In discussion. **#923** went In discussion → In progress and Future → 1.5.0, and **#961 now closes it**. **#282** went Backlog → In progress behind **#1004** (+25,745/−33, 91 files). **Coverage 4-of-33 → 1-of-41:** the old unexplained set merged or (#961) gained a closing link; the remaining miss is **#1057** (two documentation typos). **64 of 64** open issues carded. Readiness, after a cold census that warned `UNKNOWN` and a second pull once GitHub had computed mergeability: **29 BLOCKED / 10 DIRTY / 1 CLEAN (#1026, draft) / 1 UNSTABLE (#1010)**, 21 failing checks, **2 APPROVED (#459, #973)** — the open-queue approval streak broke, and neither approval is mergeable. **#1024** is board-Done and closed `NOT_PLANNED`; **#211** and **#760** are board-Done and closed unmerged; **#514** shipped via #681 and was removed from Needs review. **`php-ai-client#242` was de-carded.** Census: `WordPress/ai` **41 PRs / 64 issues / 1.3.0**; `php-ai-client` **37 / 40 / 1.5.0**; `mcp-adapter` **16 / 51 / v0.6.1** (a `ci-artifacts` prerelease on 2026-08-27 is not a product release; #288/#302/#303/#304 merged; #299 is now green and DIRTY); `abilities-api` **14 / 8 / v0.4.0 for a sixth census**. Dependency watchlist **16 (9 open / 4 closed / 3 merged) with an empty diff on every axis for a third consecutive refresh** — every `updatedAt` is still the 2026-08-17 value. **Board title and short description re-verified unchanged.** |
| 2026-08-26 | **Live refresh vs the 2026-08-23 17:04 UTC baseline — the board shrank, the embedding programme restarted uncarded, and the upstream gate was approved.** Board **291 → 290** = 228 PRs + **62** issues (mix 228/63 → 228/62); Done **218 → 216**; non-Done **73 → 74** = **56 issue cards + 18 PR cards** (17 open + stale merged #484). **Zero status changes and zero milestone changes on existing cards** — the entire delta is arrivals, one merge, and deletions. **Three added:** **#968** (arrived *already Done*, 1.4.0 — a `delete_meta` doc-comment defect filed 07:11 and closed 14:06 the same day by uncarded six-line PR #969), **#970** (In progress / 1.4.0 — the `wpai_active_seo_plugin` transient never expires, is invalidated by a hook registered only while the Meta Description experiment is on, and never caches the "no SEO plugin" result; PR #973 drew `CHANGES_REQUESTED` from dkotter within eight hours), and **#971** (Triage, unmilestoned — WouterP0lman's WCEU-originated **AI governance RFC**: local role policies, usage limits, alerts and audit data, with a working PoC its author declines to propose because its `pre_http_request` enforcement point cannot identify client, actor, feature or model — and which **names #732 as a prerequisite**, turning a logging blind spot into a governance blocker). **One newly Done / newly merged:** PR **#916** (Infinite-Null, taxonomy error copy, +223/−5) merged 2026-08-24 after **19 days**. **Four removed — the seventh de-carding pass, and the first to delete cards from a release already counted complete:** **#233**, **#690**, **#876** (all closed 2026-08-11, all milestone **1.3.0**, all delivered) and **#307** (the `AGENTS.md` proposal closed `NOT_PLANNED`). **The 1.3.0 board lane therefore reads 19 → 16 cards eight days after it closed 19/19, against a repository milestone still closed at 71.** Milestones: **1.4.0 28 → 30** (6 Done / 24 open — 15 issue cards + 9 PRs), **Future Release 42 flat on every axis**, **1.5.0 still empty**, unmilestoned **34** (28 Done / 6 open). Status mix: In progress **24 → 25**, Triage **8 → 9**, Needs review **10 → 9**, In discussion **19**, Backlog **7**, To do **5** — every move caused by an arrival or departure, none by a decision. **⚠️ Coverage regressed to its worst reading in months:** 33 open PRs = 17 direct + 12 linked + 0 routine + 0 off-board + **4 unexplained** (was 1 of 30). The new failures are **#975** (dkotter — brings the latest embedding code over from `php-ai-client#274`, re-enables `SDK_Overlay`, re-adds a temporary `wp ai embeddings generate` WP-CLI command; +1,144/−292 across 19 files, milestone 1.4.0, `[Status] Blocked`), **#976** (ColinM-sys — the portable `wpai_embeddings` storage/CRUD layer, unique key `(object_type, object_id, provider, model, chunk_index)` so an index can never be queried with another model's vectors; +2,027/−1, 51 tests, 93% patch coverage, and *not* blocked on #274), and **#977** (yogeshbhutkar — a mask-based rewrite of the Content Translation loading animation, +11/−27, green checks); **#961** (Agent Users) persists and grew **+2,439 → +3,318**. **#975 and #976 are exactly the decomposition #962 asked for, and neither is carded, neither uses a closing keyword, and #962's own checklist still shows seven unchecked boxes with no PR links.** **The gate opened:** [`php-ai-client#274`](https://github.com/WordPress/php-ai-client/pull/274) was **APPROVED by JasonTheAdams on 2026-08-25 22:23 UTC** and is now **CLEAN** (still unmerged) after a `CHANGES_REQUESTED` round and three response commits; ColinM-sys re-validated the approved head on 2026-08-26 — **1,191 tests / 4,322 assertions green on PHP 8.5**, phpstan clean, **36/36 live Ollama cases**, plus a failure-mode table settling the reviewer's open `isSupported()` catch question. dkotter opened #975 ninety minutes *before* the approval landed. **Merges: five, four uncarded, four opened and merged inside a single day** — #916 (carded, 19 days), #967 (translation title-length guard, 8h), #969 (doc comment, 7h), #972 (cap the Comment Moderation bulk queue, 10h), #974 (multisite PHPUnit CI, 5h); **all five merged by dkotter and all five carry his approving review**, while the standing queue recorded **0 of 33** — a **twelfth** consecutive window at zero, with 21 BLOCKED / 12 DIRTY, 17 failing checks, 10 CHANGES_REQUESTED, 7 drafts. **New this window: the milestone-level carding measure.** The repository's `1.4.0` milestone holds **47 items**; Project #240 cards **29**. The 18 uncarded are all PRs — eight open ones the audit counts as covered via linked issues, #975, and **nine already merged or closed** (#947, #959, #963, #965, #966, #967, #969, #972, #974). Census: `WordPress/ai` **33 PRs *(+3)* / 54 issues *(+2)* / 1.3.0**; `php-ai-client` **31 *(−1)* / 35 / 1.4.0** — **two merges** (#231 closing #230, and #275, both jeffpaul 2026-08-24, the first there in three windows) plus new issue **#281** and draft **#282** proposing *provider-agnostic deferred tool loading*, the first upstream answer to the board's oldest question **#21**; `mcp-adapter` **17 *(+4)* / 43 / v0.6.1** — justlevine filed #302/#303/#304 in three hours on 2026-08-26, **#299** fixes a 500 on `resources/read` for every ability with an object input schema, draft **#300** adds SSE streaming, dependabot #296 closed unmerged, **#288** approved; `abilities-api` **14 / 8 / v0.4.0 — static on every axis for a fifth census**, still pending archival. Dependency watchlist **16 (9 open / 4 closed / 3 merged)** with **zero drift on every axis for a second consecutive window** — not even an activity bump. **Board title and short description re-verified unchanged** ("WordPress AI Roadmap" / "A project board to provide oversight into the WordPress AI plugin"). Verification: `bash -n`, all **eight** offline fixture suites, `census --strict` (exit 0), and the live 16-ID dependency smoke test all pass; `--strict --json` exits **2** on the four `pr-roadmap-coverage-missing` errors. Script unchanged — no drift or bug found. |
| 2026-08-23 | **Live refresh vs the 2026-08-17 20:39 UTC baseline — the release window: v1.3.0 shipped, and nine of thirteen merges were never carded.** **v1.3.0 tagged 2026-08-18 21:04 UTC** — the 19th release, **one day past its 2026-08-17 target and 35 days after v1.2.0** — the longest gap since the 0.1.1 → 0.2.0 alpha stretch. jeffpaul ran the whole checklist in under three hours (release PR #958 merged 20:19 → plugin check/tests 20:56 → local testing 21:00 → GitHub release 21:04 → WPORG deploy 21:11 → [announcement](https://make.wordpress.org/ai/2026/08/18/whats-new-in-ai-1-3-0/) 22:32); release issue **#924** closed, the repo milestone closed at **71 items**, and **1.5.0 was created empty at 19:46 the same evening**. The lane closed **19/19 by deferral**: C2PA detection **#421** moved **1.3.0 → 1.4.0** rather than shipping, and its PR **#459** went **FAILURE → SUCCESS** on checks in the very window it was descoped. Board **279 → 291** = **228 PRs + 63 issues**; Done **214 → 218**; non-Done **65 → 73** = **54 issue cards + 19 PR cards** (18 open + stale merged #484). Status: In progress **24**, In discussion **19**, Needs review **10**, **Triage 3 → 8**, Backlog **5 → 7**, To do **4 → 5**. **14 added / 2 removed / 3 newly Done / 1 newly merged / 3 status / 3 milestone / 3 state.** Newly Done: **#924** (the release), **#917** (Infinite-Null's Content Resizing focus fix, merged after 15 days open), and **#940** — which closed against **uncarded PR #947**, a *timeout bump* rather than the custom settings endpoint the issue specified, and was given milestone 1.4.0 on closure. Added: merged PR cards **#941**/**#948**/**#960**; live PR cards **#943** and **#945** (last window's two unexplained PRs, now carded) and **#949** (gziolo's Knowledge/Guidelines port of Gutenberg's `lib/experimental/knowledge` + `routes/guidelines`, which also fixes a silently broken `Guidelines` service); Backlog **#951** (AI block labels in List View, from a georgestephanis Slack wish-list item); **five unmilestoned C2PA Monitor follow-ups #953–#957** filed by lnispel in one hour on 2026-08-18 — JUMBF/CBOR decoding, `@contentauth/sdk` in-browser verification, a grid indicator **blocked by the C2PA Conformance Program** (the CR trustmark is a certification mark and WordPress is not a conforming product), GD/Imagick subsize manifest stripping, and CAI Verify pre-fill — which tripled Triage; and the embeddings pair **#962** (jeffpaul's seven-step tracking issue, assigned dkotter, explicitly decomposing #683/#891/#943 away from large feature PRs) plus **`php-ai-client#242`**, a **second closed-state straggler** — In progress under 1.4.0 while closed upstream since 2026-07-15. Removed: closed issues **#193** (carded 2026-08-14, de-carded 2026-08-23 — a full add/remove cycle in nine days) and **#869**. Status moves: **#27 In discussion → To do** (open since 2025-10-06, 15 comments, still no PR or assignee), **#933 In progress → Needs review**, and **#188 In progress → Backlog**, reversing the unexplained promotion this document flagged six days ago. Milestones: #421 1.3.0 → 1.4.0, #789 Future Release → 1.4.0, #940 — → 1.4.0. **Coverage: 30 open PRs = 18 direct + 11 linked + 0 routine + 0 off-board + 1 unexplained (#961, gziolo's Agent Users PoC implementing #923's identity half without a closing link) — one strict-audit failure, the best reading yet, and misleading:** nine of the thirteen merges this window were uncarded, including **release PR #958 itself** (62 files), #947, #950, #952, #959, and dependabot #963–#966. Issue coverage **52 of 52 carded**. Readiness moved both ways for the first time in months — #459 and #749 FAILURE → SUCCESS; #887 DIRTY → BLOCKED *and* SUCCESS → FAILURE; #888 DIRTY → BLOCKED but FAILURE → SUCCESS — while the standing queue held: **0 of 30 approving reviews, an eleventh consecutive window at zero**, 19 BLOCKED / 11 DIRTY, 17 failing checks, 10 CHANGES_REQUESTED, 7 drafts. Upstream: **`php-ai-client` 29 → 32 PRs / 34 → 35 issues** (zahidgh's #278/#279/#280; new #277 — `MessagePart` cannot carry citations, so all three first-party providers drop source attribution) with **#274 still open, still BLOCKED, six days as the gate on the plugin's embedding layer** — though ColinM-sys tested it end to end against a local Ollama on 2026-08-22 (PHP 7.4–8.5 green, 36/36 live cases) and reported that it removes `usingProvider()`/`usingModelPreference()`, which `generate_embeddings()` and the #891/#943 callers both use, so re-enabling embeddings is a caller migration rather than a revert of #946. **`mcp-adapter` 14 → 13 PRs** (#291 merged, #292 closed as a duplicate) **/ 41 → 43 issues** (#297 unknown `mcp.type` fails open to `tool`; #298 `create_error_result()` discards `WP_Error` code/data), no release since v0.6.1. **`abilities-api` unchanged on every axis** since entering the census on 2026-08-10 (14 / 8 / v0.4.0), still pending archival. Dependency watchlist **16, zero drift on every axis** (9 open / 4 closed / 3 merged; no membership, state, milestone, title, or activity change). v1.3.0's published notes **kept the #892 embedding entry**, rewritten to say the code loads "conditionally" and crediting #946 — the PR that switched it off. Two dossier corrections worth recording: **#890** was recorded here as having zero comments — jeffpaul in fact replied on **2026-08-05** asking which sidebar is meant and noting it may be an upstream Gutenberg concern first, unanswered for eighteen days and the whole of that card's triage; and **#625**'s "no PR, no activity" framing is retired by a **2026-08-21 end-to-end implementation demo** posted on the issue before any PR, covering four platforms, thread mode, Mastodon content warnings, grapheme-aware counting, alt-text sync, and a `wpai_before_social_publish` hook — the board's largest unreviewed design. **#923** also drew five substantive replies (5 → 10 comments), including gziolo ruling that an editor-sidebar assistant should *not* use an agent account, and webmyc demonstrating that extending `wp_kses_allowed_html('post')` still cannot cover a third-party plugin's `<ss-admin>` web component. Registries unchanged; script unchanged (no drift found); all eight offline tests plus the live dependency smoke test pass. **Closing pull at 17:04 UTC.** The window was re-pulled live nine hours after the 07:41 read, and one thing in the entire surface had moved: board flat at 291 on every axis, both censuses flat, all four release sets flat, watchlist flat — and **PR #929 out of draft at 14:48 UTC** (zeus2611's `Stats_Provider` layer for #338, +2,907 lines across 27 files, checks already green, merge DIRTY → BLOCKED). The repository's largest contributor PR entered a review queue that has approved nothing in eleven windows; the set of open PRs that are green with no requested changes is now **eight**. One carried-over inaccuracy fixed while doing so: §6 described #929 as having "opened this window" — stale phrasing left from the 2026-08-14 window, where it did open; the bullet now dates it 2026-08-11. This window's snapshots are stamped 17:04. |
| 2026-08-17 | **Live refresh vs the 2026-08-15 00:31 UTC baseline — a frozen board, a missed release date, and a board-Done capability switched back off.** Board **flat at 279** = 222 PRs + 57 issues; Done **214**; non-Done **65** = 48 open issues + 17 PR cards (16 open + stale merged #484). Every diff category is empty except **one status change: #188** (persona-driven content generation, Future Release) **Backlog → In progress** — with no PR, no assignee, and zero comments, the third card to move this way after #338/#625 — so In progress **23 → 24** and Backlog **6 → 5**. Milestone tiers untouched (1.3.0 **19** = 17 Done/2 open, 1.4.0 **19**, Future Release **41**, no-milestone **31**, 1.2.0 **9**, 0.9.0 **19**). Board title/description re-checked by hand and unchanged ("WordPress AI Roadmap" / "A project board to provide oversight into the WordPress AI plugin."). **The board's single latest touch is not a `WordPress/ai` card at all:** Google-provider issue **#23** was de- and re-milestoned at 18:20:53 UTC as jeffpaul renumbered that repository's milestones — the milestone #23 sat on was renamed **1.2.0 → 1.1.1**, a brand-new 1.2.0 was created at 18:20:00 UTC, and #23 was moved onto it. Our recorded milestone *string* is unchanged, so the diff is correctly empty, but the label now points at a different milestone object — a reminder that milestone names are not stable identifiers across repositories. **v1.3.0's target date (2026-08-17) is this snapshot's date and the release is not cut** — 1.2.0 remains the latest tag, five weeks on; release issue **#924** carries the latest activity of any `WordPress/ai` card (17:28 UTC) and #421's PR **#459** regressed to **merge BLOCKED** on top of CHANGES_REQUESTED and failing checks, making the one substantive item left on the release checklist red on every axis. **⚠️ The window's material event is uncarded, merged, and untraceable: PR #946** (dkotter, +11/−1, repo-milestoned 1.3.0) **disabled the `SDK_Overlay` loading that board-Done #892 added four days earlier** — opened **17:27**, approved by jeffpaul **18:22**, merged into `develop` by dkotter **18:54**, all inside this refresh and **without a board card at any point**. The team wants the embedding contract changed upstream first in `php-ai-client#274`; `generate_embeddings()` now returns `WP_Error`, dependent tests are skipped, and #892's changelog entry is to be kept out of the release. #892 still reads Done, so the board is affirmatively wrong about what 1.3.0 contains — and **#683**, **#844/#891**, and **#943** are all building on a layer that no longer loads. **The tracking failure is total, not partial:** never carded, so no board diff; opened *and* merged between snapshots, so no PR-census diff row either (the baseline comparison reports only #943 and #945 as newly opened). A refresh run tomorrow finds no trace of #946 anywhere — this is the second permanently invisible merge after **#913** (2026-08-07), and the first where what vanished was the *removal* of a shipped capability. **Coverage gap 1 → 4 during the window, closing at 3:** census **28 → 30 open PRs** = **16 direct + 11 linked-board-issue + 0 routine + 1 linked-off-board (#943) + 2 unexplained (#941, #945)**, with #946 the fourth uncarded PR, gone by the closing pull. New: **#945** (seedprod, +1,347) adds a `core/edit-content` **write** ability — the twin of `core/read-content` #739 — referencing #40 without a closing link, so a new write primitive is arriving outside the #863/#923 governance frame; **#943** (ColinM-sys, +2,049) carries #891's semantic-search work plus fixes/tests from a second author, putting one experiment in two open PRs, and its only link is a *fallback-title* match to **#891**, a PR rather than an issue (the second fallback relationship in the census, after #494 → #238). Issue coverage holds at **47 of 47 carded** — the asymmetry is now the story: issue coverage is enforced and perfect while PR coverage is red on four. **Readiness degraded for everyone except the reverter.** 30 open PRs, 11 CHANGES_REQUESTED, **BLOCKED 12 → 16**, DIRTY 16 → 14, failing checks **17 → 19**, 7 drafts, and **still zero approving reviews** — a tenth consecutive window at zero, because the window's one approval went to #946 and merged out with it. Both recorded readiness changes were regressions (#459 and #749 → BLOCKED, #749 checks SUCCESS → FAILURE). Set against #946's 87-minute open-to-merge, the signal is not that review is slow but that it is **fast for maintainers and stopped for contributors**: eleven contributor PRs sit on CHANGES_REQUESTED while the one maintainer-authored change in the window shipped the day it was written. Upstream: **`php-ai-client` 28 → 29** / 34 / 1.4.0 on draft **#276** alone (JasonTheAdams, 17:50 UTC — a `token_get_all()` pass qualifying global function/constant references across 82 files, plus a `ReferenceUsedNamesOnly` phpcs rule; no behavior change, wide rebase surface), while its open **#274** did not move and remains the gate on re-enabling a merged plugin feature — the most consequential item on the radar; **`mcp-adapter` 12 → 14**, entirely documentation (**#285** readme.txt merged 2026-08-16; **#288**/**#291**/**#292** opened; draft **#281** BLOCKED → DIRTY), no release since v0.6.1; **`abilities-api` static** at 14 / 8 / v0.4.0. Dependency watchlist unchanged at **16** (9 open / 4 closed / 3 merged) with **one activity bump** — `gutenberg#75221` (Media Editor focal-point selector) → 2026-08-16, its first movement since February, on the same day its mapped card **#238** was touched. **Tooling: two bugs fixed.** `tests/helpers/crlf-jq.sh` was committed **non-executable**, so the PATH symlink the CRLF suite installs as `jq` was silently skipped and the test failed its own sanity check — invisible on Windows (msys reports every file executable), fatal on Linux, meaning that suite could never pass from a clean checkout. With the mode fixed the suite immediately caught four scalar `$(jq …)` captures that never stripped `\r`: one built a snapshot filename (`prs-WordPress-ai␍-current.json`, which killed the report with `Bad JSON in --slurpfile prs`), one built a `gh api` endpoint, and two were compared against literals (`required`, `.ok`) — all four now use `jq_lines`, so the engine no longer depends on the platform stripping CRs for it. **Two more of the same class, found by trying to run the suite from a clean environment:** three test files — `-crlf.sh`, `-issues.sh`, `-mergestate.sh`, the three newest suites — were themselves committed **non-executable**, so the invocations CLAUDE.md documents fail outright on Linux while passing on msys (which reports every file executable); and five suites shelled out to **`rg`**, which is not a documented dependency of this repo (`gh` + `jq` are) and is absent here, so they died on `rg: command not found` the moment they did become runnable. The `rg` calls are now `grep -F` — every pattern was a fixed string, so nothing about what they assert changed. Script passes `bash -n`, all **eight** offline suites, `census --strict`, `dependencies --strict --json`, and the live dependency smoke test; `--strict --json` exits 2 for #941/#943/#945. **Data-collection note:** GitHub returned intermittent HTTP 503s across its GraphQL API through the evening's earlier pulls (the device-code auth flow failed once for the same reason), and two of them lost a census entirely (`abilities-api` at 17:31, the primary `WordPress/ai` repo at 19:5x — the latter exercising the fail-soft path exactly as designed, degrading to a board-plus-three-repos report and emitting one `repository-census-unavailable` error); the **20:39 UTC census these figures come from reached all four repositories cleanly**, so all 14 snapshots roll forward together. Second caveat, newly observed: the census reads `mergeStateStatus` from a *list* query and GitHub answers those from cache, so #946 was still reported `BLOCKED` 27 minutes after its approval had made it `CLEAN` — GitHub-side lag rather than a script defect (the existing retry covers `UNKNOWN`, a genuinely uncomputed answer, not a stale computed one), but a just-changed merge state can trail the census by a window. |
| 2026-08-15 | **Live re-verification vs the 2026-08-15 00:31 UTC baseline — a true zero-movement window, plus a board-identity correction.** Board **flat at 279** = 222 PRs + 57 issues, with **every** diff category empty (0 added / 0 newly Done / 0 newly merged / 0 status / 0 milestone / 0 state / 0 removed); Done **214**, non-Done **65** = 48 open issues + 17 PR cards (16 open + stale merged #484); status, milestone, and dossier groupings all recomputed from a fresh `fetch` and unchanged. All four repository censuses are identical to the baseline — `WordPress/ai` **28 PRs / 47 issues / 19 releases**, `php-ai-client` **28 / 34 / 16**, `mcp-adapter` **12 / 41 / 7**, `abilities-api` **14 / 8 / 6** — with no PR, issue, release, or readiness change anywhere, and the 16-item dependency watchlist unchanged item-for-item (9 open / 4 closed / 3 merged). PR coverage holds at **16 direct + 11 linked-board-issue + 0 routine + 0 off-board + 1 unexplained (#941)**, all 11 links authoritative `closing` references; issue coverage holds at **47 of 47 carded**. **Correction — the source board has been renamed.** Its live title is **"WordPress AI Roadmap"**, not "WordPress AI Planning & Roadmap", and its description now reads *"A project board to provide oversight into the WordPress AI plugin."* rather than "…the various focus areas of the WordPress AI team."; the header quote here and both references in the open-issues dossier were corrected. **This class of drift is invisible to the refresh script:** snapshots store only the item array, so project-level title/description changes are never diffed and surface only on a manual check. Per the zero-change convention no snapshots were saved and no other doc was edited. Script passes `bash -n`, all **eight** offline suites, `census --strict`, and the live dependency smoke test; `--strict --json` still exits 2 for exactly #941. |
| 2026-08-14 | **Live refresh vs the 2026-08-10 11:11 UTC baseline — a shipping window: v1.3.0 drained to its release checklist, and v1.4.0 was finally loaded.** Board **269 → 279** = 222 PRs + 57 issues (mix 214/55 → 222/57); Done **202 → 214**; non-Done **67 → 65** = **48** open issues (was 51) + **17** PR cards (16 open + stale merged #484). **Nine cards reached Done, and all nine are real delivery or a deliberate decision:** seven issues closed by merged PRs — **#233** (`AI_Service` deprecation, PR #905), **#690** (uninstall cleanup, #692), **#863** (abilities toggle, #881), **#866** (`wpai_` meta prefix, #867), **#876** (permalink slugs, #897), **#906** (reserved log-type API, #914) — plus **#307 closed `NOT_PLANNED`** (the AGENTS.md proposal was declined after the April debate), and PR cards **#889/#920** merged. Five more PRs arrived already-Done under 1.3.0 (**#892**, **#909**, **#915**, **#930**, **#934**). Removed: translation issue **#187** (shipped earlier via #747, de-carded Done), webinar spam **#900**, and **PR card #459** — whose PR stays open and `closing`-linked to #421 but whose checks went FAILURE. **Tier moves: ① v1.3.0 26 → 19 carded (5 Done / 21 open → 17 Done / 2 open)** — the residue is C2PA **#421** plus release issue **#924** (target **2026-08-17**); **① v1.4.0 2 → 19** (11 issues + 8 PRs) — thirteen cards re-milestoned in from 1.3.0 (#514, #600, #660, #683, #732, #736, #741, #764, #777, #832, #845, #858, #875), joined by PR cards #888/#916/#917 and board-new **#933**; **② Future Release 42 → 41** (33 issues + 8 PRs; gained #923/#931/#890/#918, lost #600/#736 to 1.4.0 and #294/#302/#307 by milestone-clearing on Done); **④ Unscheduled 2 → 1** with board-new **#940** (custom settings endpoint vs `/wp/v2/settings`), #890/#918 now Future Release; **③ straggler #484 unchanged**. Status moves: **#600** In discussion → To do (retitled "Remove `Enable AI` header toggle…" + Help Wanted), **#514/#732** In progress → Needs review, #876 → Done. Delivery-readiness by status: In progress **29 → 23**, In discussion **20 → 21**, Needs review **6 → 8**, Backlog **6**, To do **3 → 4**, Triage **3**. **The visibility gap collapsed 6 → 1, and the six left by being tracked:** all of last window's unexplained PRs (#888/#892/#909/#915/#916/#917) were carded this window — **#892/#909/#915 merged under 1.3.0**; the sole remaining unexplained PR is **#941** "Content Translation: Add user-triggered retry" (yogeshbhutkar, repo-milestoned 1.4.0). Issue coverage is **47 of 47 open issues carded** — enforced by the audit. Readiness is unchanged in kind: 28 open PRs = **0 approvals**, 11 CHANGES_REQUESTED, BLOCKED 12 / DIRTY 16, 17 failing checks, 7 drafts; the window's six readiness changes are mostly regressions (#459/#681/#858/#888 → DIRTY, #916/#917 CHANGES_REQUESTED). **Upstream shipped for the first time:** `mcp-adapter` released **v0.6.0 (2026-08-12) and v0.6.1 (2026-08-13)** after its DTO cluster merged (#171/#223/#252/#263); open PRs 18 → **12**, open issues 46 → **41** (new #280 HTTP transport). `php-ai-client` 26 → **28** (**#274** explicit embedding model, **#275** provider-plugin updates), issues 35 → **34**. `abilities-api` frozen at 14 PRs / 8 issues / v0.4.0 (still pending archival). The dependency watchlist logged its **first state change in four windows**: `gutenberg#73771` OPEN → CLOSED (16 items now 9 open / 4 closed / 3 merged). Script passes `bash -n`, the eight offline suites, `census --strict`, `dependencies --strict --json`, and the live dependency smoke test; `--strict --json` exits 2 for exactly #941. |
| 2026-08-10 | **Live refresh vs the 2026-08-01 14:58 UTC baseline — a bookkeeping window: nothing shipped, but the board got measurably more accurate.** Board **flat at 269**, with total turnover underneath: **6 removed** (already-Done #191/#192/#452/#507/#874 from 1.3.0 + unmilestoned #883 — the sixth post-release de-carding pass) and **6 added**, so the mix moved **209 PRs + 60 issues → 214 + 55**. Done **203 → 202**; non-Done **66 → 67** = **51** open issues (was 52) + **16** PR cards (15 open + stale merged #484). Status: In progress **26 → 29**, In discussion **22 → 20**, Needs review **4 → 6**, Backlog **7 → 6**, To do **4 → 3**, Triage **3**. **Neither newly-Done card is delivery:** **#193** (developer log panel) was closed "by #437" — work already in the product — and retro-milestoned **Future Release → 1.0.0**; **#869** (provider-data iframe mismatch) was closed **`NOT_PLANNED` as not reproducible** after three windows of waiting on a clean-environment repro. Additions were four dependabot PRs (#919/#921 merged, **#922 closed unmerged yet marked Done**, #920 open), a11y PR **#889** (finally carded — it was an unexplained PR last window), and one board-new Triage issue **#918** (AI-generated `/llms.txt` site index). **All five milestone moves pulled work toward a release, reversing last window:** #875 and #876 came back from Future Release into **1.3.0** only nine days after being pushed out of 1.4.0; #233 moved in from Future Release; #906 was milestoned from nothing. **1.3.0 23 → 26 carded (8 Done/15 open → 5 Done/21 open = 13 issues + 8 PRs); Future Release 46 → 42 (44 → 40 open); 1.4.0 untouched at 2 undecided issues with no code — the reloading skipped it.** Status moves: **#338** and **#625** In discussion → In progress (neither has a PR), **#863** To do → In progress, **#760** Needs review → In progress, **#866** and **#906** In progress → Needs review. Repo census **33 → 36 open PRs** = **15 direct + 15 linked + 0 routine + 0 off-board + 6 unexplained**, and **all 15 issue links are now authoritative `closing` references** — the last fallback (#881→#863 by branch) became a real `Closes`. **The unexplained count held at six while half the set turned over, and the two worst entries resolved:** **#905** was retitled *remove* → **"Deprecate the AI_Service layer"** after dkotter argued the class and `get_ai_service()` are public surface, and now declares **`Closes #233`** — so last window's "the board can be actively wrong" case is corrected and #233 sits In progress under 1.3.0 with an authoritative closing PR; **#889** was carded; **#913 merged 2026-08-07 — uncarded**, so a default-model switch and the deprecation of the public `wpai_meta_description_result_temperature` filter entered `develop` with no board trace at all. Carried for a third window: **#888**, **#892**; still uncarded: **#909**. New and materially lighter: **#915** (Content Resizing preserves inline HTML), **#916** (taxonomy-specific classification errors), **#917** (focus Accept on completion). **Readiness did not move:** all 36 open PRs BLOCKED (24) or DIRTY (12), **zero approvals**, 16 failing checks, CHANGES_REQUESTED flat at **14**; only three real changes all window (#881 out of draft, #887 → DIRTY, #914 checks green). Upstream radar reversed direction for the first time: `php-ai-client` **24 → 26** (**#271** `isSupported()` bool contract, **#273** automatic function-call resolution loop + `withMessages()`) with the **first three APPROVED reviews the radar has recorded** (#231/#250/#254); `mcp-adapter` **20 → 19**, its first decline (#262 merged and #256 closed unmerged against draft #277 opening; #260 BLOCKED → DIRTY as the predicted overlap collided). No release anywhere for three windows. Dependency watchlist static at **16** (10 open/3 closed/3 merged) for a third window. **Tooling fix:** the census now detects GitHub's lazily-computed `mergeStateStatus: "UNKNOWN"` (27 of 36 PRs on this window's cold run), re-queries once, warns rather than errors if it persists, and ignores UNKNOWN transitions in the PR diff — without it this refresh would have logged 24 phantom readiness changes and saved them as the baseline. Script passes `bash -n`, **seven** offline fixture suites (new `-mergestate.sh`), and the live 16-ID dependency smoke test; all eight snapshots rolled forward. |
| 2026-08-01 | **Live refresh vs the 2026-07-27 08:51 UTC baseline — a re-scheduling window in which the board drifted out of sync with the repository.** Board **274 → 269** = 209 PRs + **60** issues; Done **207 → 203**; non-Done **67 → 66** (52 open issues, flat, + 15 → **14** PR cards). Status: In progress **26** and In discussion **22** flat, Needs review **5 → 4**, Backlog **7**, To do **4**, Triage **3**. The total fell through **7 de-cardings** (#614/#778/#853/#864/#865/#870/#872), not descoping. **Only three cards reached Done and only one shipped:** **#187** (multilingual rewriting/translation) closed 2026-07-28 via merged PR **#747**; PR card **#621** was **closed unmerged** after weeks of contributor silence yet marked Done with its 1.3.0 milestone stripped, so its uploads-URL hardening is *not* in `develop`; and **#900** was webinar spam carded, closed, and Done'd the same day. Two cards added — spam #900 and substantive **#906** (Request Logging has no public API for the reserved `mcp_tool`/`ability` types; PR **#914** followed on 2026-08-01 with a proper `Closes #906`). **Milestone moves ran three-to-one against the dated lanes:** #736 (1.3.0 → Future Release) and #875/#876 (1.4.0 → Future Release), all three still In progress with PRs that each drew CHANGES_REQUESTED, plus #621's milestone stripped on close. **1.3.0 27 → 23 carded (9 Done/18 open → 8 Done/15 open); 1.4.0 4 → 2, both In discussion — nothing in that lane is being built; Future Release 43 → 46 (41 → 44 open).** Repo census **32 → 33 open PRs** = **13 direct + 14 linked + 0 routine + 0 off-board + 6 unexplained** — the visibility gap doubled and changed in kind: alongside carried-over #888/#892/#889, new **#905** *deletes* the `AI_Service` layer, resolving card **#233** opposite to its own title after adoption PR #898 closed unmerged; **#913** switches default Anthropic/Google/OpenAI models, removes temperature setting, and deprecates the public `wpai_meta_description_result_temperature` filter; **#909** rewrites encryption-experiment docs and fixes caller attribution. None declares a `closing` reference. **Readiness worsened:** all 33 open PRs BLOCKED (21) or DIRTY (12), zero approvals, 17 failing checks, CHANGES_REQUESTED **11 → 14**. Upstream radar: `php-ai-client` **21 → 24** (#266/#267/#269, the last adding OCR/document parsing) / 1.4.0; `mcp-adapter` **16 → 20** (#251 and #254 merged; #258/#259 and the overlapping #260/#262/#263/#264 cluster opened) / v0.5.0 — neither cut a release. Dependency watchlist static at **16** (10 open/3 closed/3 merged) for a second window. §3 now marks `AI_Service` as being removed; §8 adds review throughput as a named risk. Script passes `bash -n`, the six offline fixture suites, and the live 16-ID dependency smoke test; all eight snapshots rolled forward. |
| 2026-07-27 | **Live refresh vs the 2026-07-20 06:15 UTC baseline — the post-1.2.0 cleanup-and-execute window.** Board **283 → 274** = 209 PRs + **65** issues; Done **209 → 207**; non-Done **74 → 67** (57 → **52** open issues + 17 → **15** PR cards). The drop is board hygiene, not descoping: **10 already-Done cards de-carded** (#508/#793/#809/#815/#816/#818/#833/#839/#846 from the shipped 1.2.0 lane + spam #848) against **1 added** (unmilestoned Triage issue **#890**, mobile right-sidebar component). **8 cards newly Done — six of them 1.3.0 issues closed by merged PRs:** #191←#734 (settings import/export), #192←#770 (prompt-template extension points), #452←#633 (taxonomy relevance), #507←#861 (Editorial Updates → Visual Revisions), #874←#886 (Yoast meta-description interop, root-caused to Yoast's own store + `post`-only REST meta), #883←#884 (Abilities Explorer custom providers + statistics); plus PR cards #882 (merged) and #851 (PoC closed unmerged). **5 status moves, all into In progress** as implementation PRs opened: #233←#898, #421←#459, #844←#891, #875←#887, #876←#897 — so **v1.4.0 is no longer purely discussion-stage**. **3 milestone moves:** #192 Future Release → 1.3.0, #874 — → 1.3.0, #851 Future Release → —. Status totals: In progress **26** (unchanged), In discussion **24 → 22**, Backlog **8 → 7**, Needs review **7 → 5**, To do **6 → 4**, Triage **3**. Milestones: 1.2.0 **21 → 13** (12 Done / 1 open), **1.3.0 25 → 27 (9 Done / 18 open)**, 1.4.0 **4** (2 In progress / 2 In discussion), Future Release **45 → 43** (41 non-Done), no-milestone **29 → 28**. Repo census **37 → 32 open PRs** = **14 direct + 15 linked + 0 routine + 0 off-board + 3 unexplained**; 12 PRs left (10 merged incl. the former audit failures **#877/#878**; #851 and #885 closed unmerged) and 7 opened. **The new unexplained set is materially bigger than the old one:** #888 adds a whole **Text to Speech** experiment (+4,216/28 files, two new abilities, gated on `ai-provider-for-openai#42` / `ai-provider-for-google#31`), #892 vendors **PHP AI Client embeddings** behind an `SDK_Overlay` (+4,394 lines) because core embedding support slipped WP 7.1 → 7.2 (`wordpress-develop#12530`), and #889 is a request-log a11y fix whose body still has an unfilled `Closes #<issue-number>`. Latest release unchanged at **v1.2.0** (2026-07-14; 18 shipped). Upstream radar moved: `php-ai-client` **20 → 21** open PRs (new #264) / 1.4.0; `mcp-adapter` **12 → 16** (new #251/#252/#254/#256) / v0.5.0. Dependency watchlist **completely static** at 16 (10 open / 3 closed / 3 merged) — no membership, state, milestone, title, or activity change. Latest board-item activity 2026-07-27 (#875). Strict audit exits `2` for #888/#889/#892; snapshots rolled forward. |
| 2026-07-20 | **Live refresh vs the 2026-07-18 16:34 UTC baseline.** No board movement: **283 items / 209 Done / 74 non-Done** (57 open issues + 17 PR cards), with every status and milestone split unchanged (1.3.0 **22** open · 1.4.0 **4** · Future Release **43**). `WordPress/ai` holds at **37 open PRs** = 16 direct + 17 linked + 2 routine + 0 linked-off-board + **2 unexplained (#877, #878)**; the only census delta is board PR **#858** (`core/read-nav-menus`), whose merge state moved **DIRTY → BLOCKED** (checks still green). Upstream radar unchanged (`php-ai-client` **20 / 1.4.0**, `mcp-adapter` **12 / v0.5.0**) and the dependency watchlist holds at **16** (10 open / 3 closed / 3 merged). Latest board-item activity advanced to 2026-07-20 (a non-status edit on 1.4.0 issue #876). Strict audit still exits `2` only for #877/#878; snapshots rolled forward. |
| 2026-07-18 | **Same-day live refresh vs the 2026-07-18 02:20 UTC baseline.** Board **282 → 283**; Done holds at **209**; non-Done **73 → 74** with new unmilestoned In-progress issue **#883**. `WordPress/ai` open PRs **35 → 37** with #884 (authoritatively closes #883) and #885 (fallback-title match to #425); coverage is now 16 direct + 17 linked + 2 routine + 0 linked-off-board + 2 unexplained. The strict audit still exits 2 only for #877/#878. Upstream repository censuses and the 16-item dependency watchlist are unchanged. |
| 2026-07-18 | **Expanded full-repository tracking.** Added declarative PR/release censuses for `WordPress/php-ai-client` (**20 open PRs; latest 1.4.0, 2026-07-15**) and `WordPress/mcp-adapter` (**12 open PRs; latest v0.5.0, 2026-04-15**) alongside the existing `WordPress/ai` census (**35; 1.2.0**). The new repositories receive normalized PR data, validation, diffs, Markdown summaries, and independent snapshots, but no Project #240 coverage requirement. Live board and dependency counts are unchanged: 282 items / 209 Done / 73 non-Done; dependency watchlist 16 (10 open / 3 closed / 3 merged). |
| 2026-07-17 | **Tracker upgrade: authoritative PR relationships + strict audit mode.** The refresh script now reads open PRs via paginated GraphQL with GitHub `closingIssuesReferences` as the authoritative PR→issue link source (fallback title/body/branch parsing is grammar-restricted and disabled for routine PRs), classifies every open PR into one of five coverage buckets (direct-board-pr / linked-board-issue / routine / linked-off-board-issue / unexplained), tracks PR readiness changes (draft/review/merge/checks), and moves the dependency watchlist into the `wp-ai-roadmap-dependencies.json` registry with resilient UNKNOWN-placeholder fetches. New `--strict` audit exits `2` on coverage/data errors after emitting the report and suppresses persistence. Live coverage at upgrade: 35 open PRs = 16 direct + 15 issue-linked + 2 routine + **2 unexplained (#877, #878)**. |
| 2026-07-17 | **Live board + repo refresh vs 2026-07-13 baseline.** Board **279 → 282**; Done **206 → 209**; non-Done remains **73**, now **56 issues + 17 PR cards** (16 open + stale merged #484). **v1.2.0 shipped 2026-07-14**; unfinished work reorganized into **1.3.0 (22 open)** and **1.4.0 (4 open)**. Added 13 cards, removed 10 already-Done cards, and moved nine baseline cards to Done. Repo census: **35 open PRs**, 19 untracked by PR card (17 substantive + 2 routine). Dependency watchlist remains 16 (10 open / 3 closed / 3 merged); Gutenberg #73771 was retitled and updated. |
| 2026-06-15 | Initial creation. Snapshot of all 245 board items; deep-read of all 58 open issues. Latest shipped v1.0.1; v1.1.0 in flight (due ~2026-06-25). |
| 2026-06-15 | Added companion **[`wordpress-ai-open-issues.md`](./wordpress-ai-open-issues.md)** — full per-issue dossiers for all 58 open issues (body + discussion + decisions + dependencies). |
| 2026-06-15 | Added companion **[`wordpress-ai-planned-work.md`](./wordpress-ai-planned-work.md)** — release-ordered delivery plan for all 69 not-shipped items incl. the 11 open PRs; surfaced in-flight experiments (Type Ahead, Service Account, WebMCP adapter, C2PA Monitor/Content/Image Provenance, Suggest Image Crops) and a deferred "Work for Later" backlog. |
| 2026-06-16 | **Refresh vs 2026-06-15 baseline** (245 → 247 items). **v1.0.2 shipped** (16th release) — a patch milestone carved out of 1.1.0 (14 items) plus #699 (from 1.2.0), so 1.1.0 → 15 open / 0 Done and 1.2.0 → 5. #699 Done. Status moves → In progress: #191, #508, #689. Board-new: #732 (Triage — Request-Log transport gap), #736 (Backlog — per-feature access controls). #641 removed from board. **Corrected §7 no-milestone list** (#690 is 1.1.0, #339 is Future Release). Companion dossiers / planned-work tables not regenerated this pass. |
| 2026-06-18 | **Refresh vs 2026-06-16 baseline** (247 → 245 items). Latest release still **v1.0.2**; board latest activity 2026-06-18. Open work now **63 non-Done items** = 53 open issues + 10 non-Done PRs. Board-Done since prior docs: #390, #391, #571, #578, #678, #701, #721. Board-new: #741 (Triage — AI/Connectors admin-page flicker). 1.1.0 now 17 items = 6 Done / 11 open; 1.2.0 now 4 open. |
| 2026-06-19 | Live GitHub refresh. Board is now **246 items** = 174 PRs + 72 issues; Done **183**, open work still **63** but now **52 issues + 11 PR cards** (10 open + stale merged #484). 1.1.0 is **8 Done / 11 open**; Future Release is **47**. Regrouped #632/#643/#741 → In discussion, #732 → Backlog, added board-tracked draft PR #683 (native vector search), and moved `WordPress/abilities-api#84` to a removed-board reference. |
| 2026-06-19 | Fresh live recheck against Project #240 plus the `WordPress/ai` repo census: no count/status drift from the earlier 2026-06-19 refresh. Verified **246** items = 174 PRs + 72 issues; Done **183** / non-Done **63**; latest release remains **v1.0.2** (2026-06-16). |
| 2026-06-19 | Expanded tracked landscape with a separate Gutenberg + abilities-api dependency watchlist: 16 dependencies (11 Gutenberg, 5 abilities-api; 10 open, 3 closed, 3 merged). Project #240 counts remain unchanged and separate. |
| 2026-06-20 | **Live refresh vs 2026-06-19 baseline** (246 → 245 items = 174 PRs + 71 issues). Done **180** / non-Done **65** (54 open issues + 11 PR cards). Three closed Request-Log bug issues (#666/#667/#670, milestone 1.0.2) left the board → 1.0.2 14→11, Done issues 20→17. Two board-new **unmilestoned** In-progress bug fixes added: #750 (guest-comment moderation, PR #751) + #752 (Request-Log 30-day window, PR #753). Status moves Backlog → In progress: #187, #736. In progress 12→16 issues; Backlog 12→10. 1.1.0 holds at 8 Done / 11 open; Future Release **47**; latest release still **v1.0.2** (2026-06-16). Dependency watchlist unchanged at 16 (10 open / 3 closed / 3 merged). |
| 2026-06-25 | **Live refresh vs 2026-06-20 baseline** (245 → 250 items = 178 PRs + 72 issues). Done **183** / non-Done **67** (53 open issues + 14 PR cards = 13 open PRs + stale merged #484). Status moves: In progress 21→23, Backlog 10→8, Needs review 5→7. **Newly board-Done:** #632 (deactivate-connector settings retention), #750 (guest-comment moderation), #752 (Request-Log 30-day window). **Board-new open bugs (unmilestoned):** #767 (locale-aware content gate disables AI buttons for CJK content; In discussion) + #771 (Content Classification suggestion pill lost on add-failure; In progress, draft PR #772). Status moves Backlog → In progress: #192 (PR #770), #732 (draft PR #757). Milestone moves: #614 & #741 → 1.1.0, #683 → 1.2.0, #621 → Future Release, #750 → 1.1.0. Removed from board (closed Done): #662, #668, #684, #693. Milestones: 1.0.2 **11→8** (#662/#668/#693 left), **1.1.0 now 27 = 13 Done / 14 open** (due 2026-06-25, today), 1.2.0 **4→6**, Future Release **47→45**. Done issues 17→19. Latest release still **v1.0.2** (2026-06-16; 16 shipped). New board PRs: #739 (core/content ability), #764 (core/manage-settings ability), #765 (Repo Automator), #766 (editor support in readmes, Done). Dependency watchlist unchanged at 16 (10 open / 3 closed / 3 merged); `WordPress/gutenberg#77230` retitled. |
| 2026-06-30 | **Live refresh vs 2026-06-26 baseline** (249 → 269 items = 193 PRs + 76 issues). Done **183 → 195** / non-Done **66 → 74** (54 open issues + 20 PR cards = 19 open PRs + stale merged #484). **1.1.0 all but shipped: 14 Done/13 open → 27 Done/2 open**, release now tracked in **#805** (target **2026-07-30**); the bulk of 1.1.0's remaining work was **re-milestoned to 1.2.0**, which becomes the active build wave at **0 Done / 31 open**. **3 issues newly board-Done:** #145 (register() rename), #767 (CJK content gate), #771 (Content-Classification pill). 4 new open issues: #778 (E2E locators), #791 (Type Ahead cursor), #793 (Customize-experiments tool), #805 (release tracker). Status moves: #187/#508 In progress → Needs review; #197 To do → In progress; #690 Needs review → In progress; #765 Needs review → In progress. 23 milestone moves (mostly 1.1.0 → 1.2.0). Status totals: In progress 22→28, Needs review 7→9 (In discussion 21, Backlog 8, To do 7, Triage 1 unchanged). Milestones: 1.0.2 **8→5** (#699/#704/#718 de-carded), no-milestone **17→18** (now all Done). Latest release still **v1.0.2** (2026-06-16; 16 shipped). Repo census: **35 open PRs / 16 untracked** (16 substantive + 0 routine); six former off-board PRs (#560/#695/#758/#760/#774/#777) became board-tracked. Dependency watchlist unchanged at 16 (10 open / 3 closed / 3 merged); 3 activity bumps (`gutenberg#73771`, `#77230` → 2026-06-30, `#77816` → 2026-06-29). |
| 2026-06-26 | **Live refresh vs 2026-06-25 baseline** (250 → 249 items = 178 PRs + 71 issues). Done holds at **183** / non-Done **67 → 66** (53 open issues + 13 PR cards = 12 open PRs + stale merged #484). **PR #151 (Type Ahead) merged board-Done** into 1.1.0 (OPEN → MERGED; In progress → Done) — the experiment's first appearance in Done. **#767** (locale-aware content gate) moved **In discussion → To do** and was milestoned **1.2.0**. Board-Done bug **#697** (excerpt focus loss; closed 2026-06-11) **de-carded** from the board. Status: In progress 23→22, In discussion/Needs decision 22→21, To do 6→7 (Done/Backlog/Needs review/Triage unchanged). Milestones: **1.1.0 now 14 Done / 13 open** (due 2026-06-25, **now past**), 1.2.0 **6→7**, no-milestone **19→17** (16 Done / 1 open). Done issues 19→18. Latest release still **v1.0.2** (2026-06-16; 16 shipped). Repo census: **37 open PRs / 25 untracked** (24 substantive + 1 routine); newly opened off-board #773–#777 (incl. #774 `core/users`, #776 Type Ahead follow-up). Dependency watchlist unchanged at 16 (10 open / 3 closed / 3 merged); 1 activity bump (`WordPress/gutenberg#77994` → 2026-06-25). |
| 2026-07-03 | **Live refresh vs 2026-07-02 baseline** (271 → 265 items = 194 PRs + 71 issues). Done **196 → 189** / non-Done **75 → 76** (55 → **56** open issues + 20 non-Done PR cards). **7 already-Done issues de-carded from the board:** #390/#391/#571/#578/#678 (1.1.0) and #589/#727 (1.0.2) — all shipped, just no longer board cards, so 1.1.0 **27 → 22** and 1.0.2 **5 → 3**. **1 status move:** #816 (Type-Ahead front-end regression) **Triage → In progress** (fix PR #820). **1 board-new issue:** #818 (missing alt text on the AI Home feature-card `<img>`; In progress, no milestone, PR #819). Status totals: In progress **27 → 29**, Triage **2 → 1** (In discussion 21, Needs review 10, Backlog 7, To do 8 unchanged). Milestones: no-milestone **22 → 23** (20 Done / 3 open — #809, #816, #818); 1.2.0 holds at **32**, Future Release at **40**. Repo census: **47 open PRs / 28 untracked** (21 substantive + 7 routine dependabot; 19 board-tracked) — up from 35/16 as PRs #817/#819/#820 (+ dependabot #821–#827, #828, #829) opened. Latest release still **v1.1.0** (2026-07-01; 17 shipped). Dependency watchlist unchanged at 16 (10 open / 3 closed / 3 merged). |
| 2026-07-02 | **Live refresh vs 2026-06-30 baseline** (269 → 271 items = 194 PRs + 77 issues). Done **195 → 196** / non-Done **74 → 75** (55 open issues + 20 PR cards = 19 open PRs + stale merged #484). **🚀 v1.1.0 SHIPPED (2026-07-01)** — the 17th release: encryption PR **#560** merged (2026-06-30), release issue **#805** closed board-Done, and the credentials gate **#197** closed board-Done (milestone cleared). **1.1.0 29 → 27** (all Done; de-carded #701/#721 left the milestone); **1.2.0 is now the sole active build wave, 31 → 32 open**. 4 new open items: **#809** (Content-Summary nested-block detection, In progress, PR #810), **#814** (feature-request-template markdown fix, Needs review PR, 1.2.0), **#815** (Connector-Approvals access-notice gap, To do, 1.2.0), **#816** (Type-Ahead front-end `wp-editor` load breaks WooCommerce checkout — Triage, unmilestoned; a post-1.1.0 regression). Status moves: **#190** Backlog → To do. Status totals: In progress 28→27, Needs review 9→10, Backlog 8→7, To do 7→8, Triage 1→2 (In discussion 21 unchanged). Milestones: no-milestone **18 → 22** (20 Done / 2 open — milestone-cleared #197 + #632 joined Done; #809 + #816 are the 2 open); Future Release holds at **40** (#632's milestone cleared on close). Repo census: **35 open PRs / 16 untracked** (16 substantive + 0 routine); off-board #810 opened (Closes #809), #799 closed. Dependency watchlist unchanged at 16 (10 open / 3 closed / 3 merged); 2 activity bumps (`WordPress/gutenberg#73771`, `#77230` → 2026-07-01). |
| 2026-07-09 | **Live refresh vs 2026-07-03 baseline** (265 → 277 items = 205 PRs + 72 issues). Done **189 → 200** / non-Done **76 → 77** (56 → **57** open issues + 20 non-Done PR cards = 19 open + stale merged #484). **🚧 v1.2.0 opened its first board-Done batch (0 Done / 32 → 10 Done / 31 open; 41 cards):** #774 (core/read-users ability, merged), #814 (feature-request-template markdown fix), #816 (Type-Ahead front-end/WooCommerce regression, closed), #818 (AI-Home alt-text a11y), #833 (Title-Generation MutationObserver crash), #838 (screenshots), #839 (Type-Ahead Escape-restart), #846 (Type-Ahead ghost-text overlap), plus CI/dep PRs #821/#831. **5 already-Done issues de-carded:** #750/#755/#763/#768 (1.1.0) + #752 (no-milestone) → 1.1.0 **22 → 18**. **Milestone move:** PR #765 (Add Repo Automator action) 1.2.0 → Future Release. **Status moves To do → In progress:** #793 (Customize-experiments tool), #815 (Connector-Approvals notice). **2 board-new Backlog experiments** (Future Release): #844 (semantic search in wp-admin), #845 (Markdown feeds via `html-to-md`). **Spam:** #848 (Triage, no milestone) landed 2026-07-09 — pending removal. Status totals: Done 189→200, In progress 29→30, Backlog 7→9, To do 8→6, Triage 1→2, Needs review 10→9 (In discussion 21 unchanged). Milestones: Future Release **40 → 43** (33i+7PR → 35i+8PR), no-milestone **23 → 27** (25 Done / 2 open — #809, #848). Repo census: **38 open PRs / 19 untracked** (all substantive, 0 routine — dependabot PRs now carded and #831 pins/ignores `@wordpress`); latest release still **v1.1.0** (2026-07-01; 17 shipped). Dependency watchlist unchanged at 16 (10 open / 3 closed / 3 merged); 1 activity bump (`gutenberg#73771` → 2026-07-08). |
| 2026-07-12 | **Live refresh vs 2026-07-09 baseline** (277 → 279 items = 206 PRs + 73 issues). Done **200 → 206** / non-Done **77 → 73** (57 → **53** open issues + 20 non-Done PR cards = 19 open + stale merged #484). **🚧 v1.2.0 kept shipping (10 Done / 31 open → 14 Done / 28 open; 41 → 42 cards):** +4 board-Done — **#508** "Suggest Reply" experiment (PR #724 merged 2026-07-10, the lane's first genuinely new experiment), **#793** Customize-experiments tool (#842), **#815** Connector-Approvals notice (#830), dependabot **#837** — plus the no-milestone **#809** nested-block fix (#810). **1 new PR card:** #857 (Add/commit access docs, Needs review, 1.2.0). **Board-new / moved:** #853 (Generate_Image hardcoded-timeout bug) added to **Triage** (no milestone); **#845** (Markdown feeds) moved **Backlog → In progress** as implementing off-board PR #855 opened. **Spam #848 closed** board-Done (as "completed"), leaving Triage. Status totals: Done 200→206, In progress 30→28, Backlog 9→8, Needs review 9→8, To do 6, Triage 2 (now #40 + #853), In discussion 21 unchanged. Milestones: 1.2.0 **41 → 42**, no-milestone **27 → 28** (27 Done / 1 open — #853), Future Release holds at **43** (#845 moved within it). Repo census: **38 → 37 open PRs / 18 untracked** (all substantive, 0 routine); #724/#810/#830/#842 merged out, board-new PRs #855/#856/#858 in. Latest release still **v1.1.0** (2026-07-01; 17 shipped) — a **1.1.1** patch for the shipped Type-Ahead regression remains undecided. Dependency watchlist unchanged at 16 (10 open / 3 closed / 3 merged); 1 title change + 1 activity bump (`gutenberg#77230` retitled "…WordPress 7.1" → "…7.2", → 2026-07-11). |

<!--
MAINTENANCE NOTES (not rendered):
- "WordPress AI" board == the WordPress/ai Showcase Plugin in practice (307/309 items; the others are Google #23 and php-ai-client#284).
- Roadmap signal = open ISSUES plus the non-Done PR lane; PRs are most of the 214 Done cards.
- A Done card can be undone by a LATER UNCARDED PR: #892 (embeddings) merged Done 2026-08-13, #946 disabled it 2026-08-17. When a Done card matters, check the repo for a follow-up that reverses it — the board will not show one.
- Project-level title/description drift is invisible to the diff (snapshots store only the item array); re-check by hand each window.
- Board total can FALL without scope loss: post-release the team de-cards finished milestone cards in bulk (10 removed 2026-07-27, 6 on 2026-08-10). A RISE can be real delivery — 2026-08-14 was 269 → 279 with 13 added (5 already-Done 1.3.0 PRs carded) and 3 removed. Always read .board.added and .board.removed, never the count alone.
- Dependabot PRs are carded now, so `routine` reads 0 while dependency PRs still enter Done (2 merged + 1 closed-unmerged on 2026-08-10). Routine==0 does not mean "no dependency churn".
- Priority field is barely used; track via Status + Milestone.
- Strategic bets (⭐): #348, #40, #430, #297, #324, #338, #189, #282. Risk (⚠️): #448 (WebMCP), #643 (live regression).
- Watch core deps: WP 6.9 Abilities API, WP 7.0 AI Client, Gutenberg RTC/Media Editor/Guidelines→Skills, abilities-api repo.
-->
